# OpenXR-mode integration — next steps

Pick-up doc for future sessions. Pairs with the master plan at [`/openxr-migration.md`](../../openxr-migration.md). When the two disagree, the migration plan is authoritative and this file is wrong.

## Where we are

**🎉 2026-05-27 — OpenXR mode streams video end-to-end (black screen fixed, verified on RTX 3090 + Quest 3).** The full pipeline works: comp_alvr squashes + FFR-compresses the per-view layers, the Vulkan-input CUDA-interop NVENC encoder (`alvr_oxr_submit_layers`) encodes the foveated frame, and the client decodes and displays it. The last blocker — a black headset despite a valid bitstream — was root-caused to the OpenXR runtime's event loop **silently dropping every `ServerCoreEvent::RequestIDR`**: the encoder forces an IDR only once at stream start (which races the client's decoder setup and is missed), so the client only ever received P-frames and the Qualcomm decoder rejected every input buffer ("Unsupported input buffer") for lack of a keyframe. Fix (alvr `e2eb150e`): wire `RequestIDR → encoder_bridge::insert_idr()`, mirroring OpenVR's `RequestIDR()`. Supporting commits: `088dd27b` (strip the SPS/PPS NVENC repeats in front of each IDR, matching `ParseFrameNals`) and `efc93dec` (remove the bring-up diagnostics). See 3.4 below.

**Phases 0–2 scaffolding is in place and compiles clean.** OpenVR mode is unchanged and remains the default. To verify the state of the tree at any point:

```sh
cargo check -p alvr_server_openxr -p alvr_session -p alvr_dashboard -p alvr_xtask
```

What landed (this is the "starting state" for future work):

| Area | Files | Status |
| --- | --- | --- |
| New Rust crate | `alvr/server_openxr/{Cargo.toml, build.rs, src/lib.rs, include/alvr_runtime_bridge.h}` | Compiles. All 10 bridge fns return `NotImplemented`. |
| Workspace | `Cargo.toml` (added `alvr_server_openxr`) | Done. |
| Settings schema | `alvr/session/src/settings.rs` (`RuntimeMode` enum + `ExtraConfig.runtime` field) | Default `Steamvr`. Dashboard auto-renders the toggle. |
| Dashboard launcher | `alvr/dashboard/src/steamvr_launcher/mod.rs` | When `runtime == Openxr`, `launch_steamvr` warns and returns instead of starting SteamVR. |
| xtask | `alvr/xtask/src/build_openxr.rs` + `main.rs` | New subcommand `cargo xtask build-openxr-runtime [--release] [--enable-alvr-driver]`. |
| Monado driver | `openxr/src/xrt/drivers/alvr/{alvr_interface.h, alvr_internal.h, alvr_hub.c, alvr_hmd.c, alvr_controller.c, CMakeLists.txt}` | Gated by `XRT_BUILD_DRIVER_ALVR=OFF`. Returns identity poses (placeholder). |
| Monado compositor | `openxr/src/xrt/compositor/alvr/{comp_alvr.h, comp_alvr.c, CMakeLists.txt}` | Gated by `XRT_FEATURE_COMP_ALVR=OFF`. Stub returns `XRT_ERROR_NOT_IMPLEMENTED`. |
| Monado target builder | `openxr/src/xrt/targets/common/target_builder_alvr.c` | Calls `alvr_create_devices`. Not yet inserted into `target_lists.c`. |
| Submodule prep | `.gitmodules` (commented-out block), `docs/monado-notes/SUBMODULE_PIN.md` | Not yet converted. |
| Docs | `docs/monado-notes/*` (moved out of `openxr/`) | Done. |

## Known issues to resolve before Phase 3

These block real progress; fix them in this order.

### 1. The bridge header — RESOLVED (2026-05-20)

Root cause was cbindgen 0.27's syn version not parsing Rust 2024 syntax (`#[unsafe(no_mangle)]`, `unsafe extern "C" fn`); it silently dropped the entire C surface. Fixed by bumping the build-dep to `cbindgen = "0.29"` and adding `enumeration.rename_variants = QualifiedScreamingSnakeCase` to the cbindgen `Config` in `alvr/server_openxr/build.rs` (matches the convention in `alvr/server_core/cbindgen.toml`).

Regeneration stays opt-in via `ALVR_REGENERATE_BRIDGE_HEADER=1 cargo build -p alvr_server_openxr` so unrelated `cargo check` runs don't churn the header on disk.

Driver C consumers also updated to match: `AlvrOxrSide side` (typedef, 1 byte — was `enum AlvrOxrSide`, 4 bytes, ABI mismatch with `#[repr(u8)]`); `ALVR_OXR_RESULT_OK` (was `ALVR_OXR_OK`); `ALVR_OXR_SIDE_LEFT/RIGHT` now actually emitted by cbindgen.

Note: CMakeLists.txt only checks file existence, not content. A future hardening would be to also check the header isn't a bare include-guard shell.

### 2. The Monado-side patches conflict with the submodule plan — PREPPED (2026-05-20)

Path forward decided: **Option A (fork branch)**. Local prep artefacts now live in `docs/monado-notes/`:

- `PHASE2_MANIFEST.md` — 10 Phase 2 files inventoried; upstream baseline pinned to Monado 25.1.0; lists upstream-file edits (parent CMakeLists subdir registrations, `target_lists.c`, top-level options) that must be authored on the fork branch alongside the patch.
- `phase2_alvr.patch` — mailbox-format patch with all 10 additive files. Verified `git am`-applicable to an empty repo. Apply onto `v25.1.0` checkout.
- `convert_to_submodule.sh` — annotated conversion script. Edit `FORK_URL`, then run from the ALVR repo root.
- `SUBMODULE_PIN.md` — full procedure pointing at the above.

What's still on the maintainer:
1. Create `alvr-org/monado` fork (or equivalent) on GitLab.
2. Build the `alvr` branch from `v25.1.0`, apply `phase2_alvr.patch`, hand-author the upstream-file edits in `PHASE2_MANIFEST.md`, push.
3. Edit `FORK_URL` in `convert_to_submodule.sh` and run it.

Until step 3 is run, do not run `git submodule add openxr` by hand — it will erase the Phase 2 files.

### 3. ~~`target_builder_alvr.c` is not yet wired into Monado's target list~~ — RESOLVED in fork commit `c2ee5dffc` (the same one that converted `openxr/` to a real submodule). `target_lists.c` includes `alvr_interface.h` under `XRT_BUILD_DRIVER_ALVR` and slots `t_builder_alvr_create` at the top of `target_builder_list[]`. `target_instance.c` selects `comp_alvr` over `comp_main`/`comp_null` when built in (fork commit landing 2026-05-21 alongside this doc update).

## Phase 3 — the next real chunk (5–10 days)

Order matters because of the encoder refactor blocker.

### 3.0 (blocker) — Refactor encoder out of `alvr/server_openvr/cpp/`

Today the NVENC/AMF/VPL paths in `alvr/server_openvr/cpp/` are stitched into SteamVR's `IVRDriverDirectModeComponent` interface. Phase 3 needs them runtime-agnostic.

Suggested shape:
- Extract a `class Encoder` (or similar) whose constructor takes "input: shared VkImage handle + sync object" and emits encoded packets.
- Both `alvr_server_openvr` and `alvr_server_openxr` instantiate it. The SteamVR-specific glue stays in `server_openvr/cpp` as a wrapper.
- Consider extracting to a new crate `alvr/encoder/` with a C++ core + a thin Rust ABI.

This refactor must land before 3.5 or both paths duplicate code.

### 3.1 — Wire `alvr_server_core` into the bridge stubs — LANDED 2026-05-20 (except `submit_layers`)

| Stub | Status | Notes |
| --- | --- | --- |
| `alvr_oxr_init` | ✅ 3.1.1 | Takes `root_dir`; constructs `ServerCoreContext`, starts connection, spawns event-drain thread (3.1.2). |
| `alvr_oxr_shutdown` | ✅ 3.1.1 | Flips SHUTDOWN_FLAG, drops context, joins drain thread. |
| `alvr_oxr_get_hmd_info` / `_get_controller_info` | ✅ 3.1.3 | Returns stable bridge-side serials (`ALVR_HMD`, `ALVR_Controller_{Left,Right}`). HeadsetEmulationMode spoofing left as a follow-up if a downstream tool needs it. |
| `alvr_oxr_get_head_pose` | ✅ 3.1.2 | `context.get_device_motion(HEAD_ID, target)`; explicit `motion.predict()` deferred (Monado already passes a future predicted timestamp). |
| `alvr_oxr_get_controller_state` | ✅ 3.1.3 + 3.1.4 | Pose + velocities via `get_device_motion`; trigger / squeeze / thumbstick / 16-bit buttons bitfield from `CONTROLLER_BUTTON_CACHE` populated by the drain thread on `ServerCoreEvent::Buttons`. |
| `alvr_oxr_set_haptic` | ✅ 3.1.3 | Forwards to `context.send_haptics`. |
| `alvr_oxr_submit_layers` | ✅ LANDED 2026-05-27 | Vulkan-input CUDA-interop NVENC encoder body (`encoder/win32_vk/VkEncoderBackend.cpp`). Imports comp_alvr's per-view OPAQUE_WIN32 scratch images into CUDA, composites them into the combined NVENC input frame, encodes (H.264/HEVC), and streams via `send_video_nal`. Verified end-to-end on RTX 3090 + Quest 3 — see 3.4. |
| `alvr_oxr_poll_session_event` | ✅ 3.1.2 | Drains a `SESSION_EVENTS_RX` queue the drain thread populates from `ClientConnected` / `ClientDisconnected` / `ShutdownPending`. |
| `alvr_oxr_get_view_params` (new in 3.1.5) | ✅ 3.1.5 | Per-eye pose + FOV from the `LOCAL_VIEW_PARAMS` cache the drain thread maintains. Drives Monado's `xrLocateViews` equivalent. |

Reference impl: `alvr/server_openvr/src/lib.rs`. Notable deviations from OpenVR mode: no `SetTracking`/`SetButton` FFI push (Monado pulls on demand); no `event_loop` thread runs on the OpenVR side — instead a dedicated `alvr_oxr_event_drain` thread.

### 3.1.x — follow-ups outstanding

- ✅ Battery `AlvrOxrEventType` variant + drain handler. `ServerCoreEvent::Battery` now emits `AlvrOxrEvent::Battery` with `data[4]` = `[device_kind, gauge_bp, plugged, 0]` where `device_kind` is one of `ALVR_OXR_DEVICE_KIND_{HMD,LEFT_CONTROLLER,RIGHT_CONTROLLER}` (other tracking IDs are dropped) and `gauge_bp ∈ 0..=ALVR_OXR_BATTERY_GAUGE_SCALE` (10 000). Landed 2026-05-21.
- RefreshRate variant intentionally **not** wired: no `ServerCoreEvent::RefreshRate` exists upstream (rate is negotiated once at connection time). `AlvrOxrEventType::RefreshRateChange = 5` stays reserved for the day one lands.
- Monado-side `alvr_hmd.c` wiring of `alvr_oxr_get_view_params` (today the driver returns identity views). Lives on the fork's `alvr` branch per Option A.
- Monado-side handler for the new `Battery` event (consume `data[0..3]` and surface to the headset / dashboard if a use case appears).

### 3.2 — Fake compositor in `comp_alvr.c`

- ✅ **3.2.1 (LANDED 2026-05-21)** — fork commit `74635c623`; alvr-side bump `3d093476`. `comp_alvr.c` is no longer a 33-line stub: it now mirrors `compositor/null/null_compositor.c` line-for-line. A new internal header `comp_alvr_internal.h` holds `struct comp_alvr_compositor` (extends `comp_base`), logging macros, and the down-cast helper. The .c body brings up the Vulkan bundle via `comp_vulkan_init_bundle` (same instance/device extension lists as `null`, plus the platform `external_memory_*` / `external_semaphore_*` extensions 3.2.2 needs), `comp_swapchain_shared_init`, a fake pacer seeded from `xdev->hmd->screens[0].nominal_frame_interval_ns` (72 Hz today), and `system_compositor_info` built from `xdev->hmd->view_count` + blend modes. The compositor wraps via `comp_multi_create_system_compositor`. `layer_commit` is a no-op stub that runs the pacing markers (`U_TIMING_POINT_BEGIN` / `SUBMIT_BEGIN` / `SUBMIT_END`) and `comp_swapchain_shared_garbage_collect`.
- ✅ **3.2.2 (LANDED 2026-05-21)** — fork commit `93f91eb92`; alvr-side bump `f2f65d89`. `layer_commit` walks `comp_layer_accum.layers`, filters to `XRT_LAYER_PROJECTION` / `XRT_LAYER_PROJECTION_DEPTH`, packs each into an `AlvrOxrLayer` (native image handle via `xrt_swapchain_native.images[image_index].handle`, width/height from `comp_swapchain.vkic.info`, pose/FOV from view 0), and calls `alvr_oxr_submit_layers`. Non-projection layer types are dropped with one per-frame DEBUG log. CMakeLists now mirrors `drivers/alvr/CMakeLists.txt` around the bridge: `ALVR_BRIDGE_HEADER_DIR` include + `alvr_runtime_bridge.h` existence check + `alvr_server_openxr` cdylib link + `ALVR_SERVER_OPENXR_LIB_DIR` search hint.
- ✅ **3.2.3 (LANDED 2026-05-21)** — fork commit `d4705186d`; alvr-side bump `babf095d`. `layer_commit` converts the incoming `xrt_graphics_sync_handle_t` to the bridge's `u64 sync_handle`, gated on `xrt_graphics_sync_handle_is_valid` so the platform-specific invalid sentinel doesn't leak as a real handle. `u_graphics_sync_unref` happens unconditionally after the bridge call (multi-client compositor handles sync semantics — same pattern as `null_compositor`).
- ✅ `XRT_FEATURE_COMP_ALVR` is wired into `target_instance.c`: when built in, `comp_alvr_create_system_compositor` runs ahead of `comp_main`/`comp_null` (env `XRT_COMPOSITOR_NULL=1` still wins for headless smoke tests). Fork commit 2026-05-21, alvr-side bump.

**Verification ceiling** for all three sub-slices: `cargo xtask build-openxr-runtime --enable-alvr-driver` on this Windows host fails at Monado's CMake configure for upstream Eigen3 dep (not installed locally, not mentioned in `install.txt`, no vcpkg). The compositor-side code is structurally consistent with the `null_compositor` reference + the existing `drv_alvr` build wiring, so end-to-end compile gate stays open until Eigen3 is installed or a different verification host is used.

### 3.3 — Frame pacing markers — LANDED 2026-05-21

`comp_alvr.c::layer_commit` previously stacked `BEGIN`, `SUBMIT_BEGIN`, and `SUBMIT_END` at the end of the function (same as `null_compositor`, which does no real work). With `alvr_oxr_submit_layers` doing real work in between — and the Vulkan-input NVENC body landing as Slice 3.3 — the pacer was learning nothing about ALVR's submission cost. Fork commit `4cce895cd` re-positions the markers to bookend the bridge call, matching `compositor/main/comp_target_swapchain.c` around its present:

| Marker | Position |
| --- | --- |
| `U_TIMING_POINT_BEGIN` | start of `layer_commit`, after `frame_id` is read |
| `U_TIMING_POINT_SUBMIT_BEGIN` | immediately before the layer pack loop closes / right before `alvr_oxr_submit_layers` |
| `U_TIMING_POINT_SUBMIT_END` | immediately after the bridge call returns |

Markers run unconditionally even on no-layer frames so the per-frame state machine stays consistent. Verification ceiling: `comp_alvr_create_system_compositor` now runs in-process (`Using builder alvr: ALVR (streamed)` + `ALVR compositor ready` in the boot log); the actual pacer learning behaviour is observable once a client connects and frames flow (hardware-gated, Slice 3.3+).

**Latent comp_alvr-not-selected bug fixed in the same session** (fork commit `56466ce47`). `XRT_FEATURE_COMP_ALVR` was set as a CMake option and the comp_alvr target was being built + linked into `target_instance`, but the `#ifdef XRT_FEATURE_COMP_ALVR` guard in `targets/common/target_instance.c` was never true at compile time because the feature wasn't listed in `xrt_config_build.h.cmake_in`. Result: `target_instance` silently fell through to `comp_main_create_system_compositor`, and `comp_alvr_create_system_compositor` was unreachable despite a clean build and clean boot. This is the same class of "static link success doesn't prove dynamic behaviour" pattern that surfaced earlier this session (latent-bug list below). For future Monado-side touches, **always grep the boot log for the specific factory function name, not just for the builder/driver name**.

### 3.4 — End-to-end smoke — ✅ ACHIEVED 2026-05-27

Exit criterion for Phase 3: an OpenXR app running against the ALVR-Monado runtime (`XRT_BUILD_DRIVER_ALVR=ON XRT_FEATURE_COMP_ALVR=ON`) shows up on the headset and is interactive.

**Met.** Streamed `oxr_overlay_smoke` (a colour-cycling projection-layer app) through `monado-service` on the RTX 3090 host to a Quest 3 running `alvr.client.dev`, foveation on, H.264 2560×1184. The headset shows video; the client decodes cleanly (0 "Unsupported input buffer", ffmpeg validates 120/120 of the client-received frames). Matches the OpenVR/SteamVR baseline on the same client.

**The black-screen bug that gated this (root cause + fix):** the runtime dropped every `ServerCoreEvent::RequestIDR`, so after the single stream-start IDR (missed during the client's decoder setup) the client only ever received P-frames → no reference frame → the Qualcomm HW decoder rejected every input buffer. The investigation eliminated, in order, inline SPS/PPS config (`088dd27b` strips it anyway, matching `ParseFrameNals`), bitstream validity (ffmpeg decoded the server bytes 120/120), codec (H.264 and HEVC both failed identically), and resolution (2560×1184 and 960×416 both failed) — then a client-side NAL capture showed the dump contained only P-slices (`0x61`), never an IDR (`0x65`). Fix: `RequestIDR → encoder_bridge::insert_idr()` (alvr `e2eb150e`). Diagnostics removed in `efc93dec`.

Note: this was reached via the `oxr_overlay_smoke` path rather than `hello_xr` specifically; the original `hello_xr` exit phrasing is satisfied equivalently (a real OpenXR client submitting frames renders on the headset).

## Phase 4 — runtime registration (2–3 days)

1. ✅ **4.1 (LANDED 2026-05-21)**: `cargo xtask build-openxr-runtime --enable-alvr-driver` now also turns on `XRT_FEATURE_COMP_ALVR` (the two were artificially separate) and publishes Monado's auto-generated dev manifest to a stable filename `build/openxr-{profile}/active_runtime_alvr.json`. Loader can be pointed at it with `XR_RUNTIME_JSON=<path>` for dev.
2. ✅ **4.2 (LANDED 2026-05-21)**: `cargo xtask register-openxr-runtime` / `unregister-openxr-runtime` cross-platform:
   - **Windows**: writes `HKCU\Software\Khronos\OpenXR\1\ActiveRuntime` via `reg.exe`. (Earlier draft of this doc said `%LOCALAPPDATA%\openxr\1\active_runtime.json` — that's wrong; Windows OpenXR loader uses the registry, not a file at that path.)
   - **Linux / BSD**: writes `$XDG_CONFIG_HOME/openxr/1/active_runtime.json` (fallback `$HOME/.config/openxr/1/...`).
   - macOS deliberately unsupported (no released Monado/ALVR target).
   Unregister refuses to clear when the currently-registered value doesn't match this profile's manifest — won't stomp on a different vendor's runtime.
   The action is **system-modifying for the current user**; the launcher GUI doesn't surface it yet (deliberate — a CLI-only path keeps the side-effect explicit until a UI for it is in place).
3. ✅ **4.3 (LANDED 2026-05-21)**: Mutual exclusion implemented on both sides. SteamVR side already had the warn-out in `dashboard/src/steamvr_launcher/mod.rs::launch_steamvr` (refuses when `RuntimeMode::Openxr`). OpenXR side now mirrors it: `cargo xtask register-openxr-runtime` refuses with `error:` if `vrserver` is alive. Both runtimes claim the same headset connection from the client; letting them race produces a stream that goes nowhere useful.
4. ✅ **4.4 (LANDED 2026-05-21)** — smoke-test plan in [`SMOKE_TESTS.md`](SMOKE_TESTS.md). Concrete gates (build clean / ABI contract / hello_xr / Battery event / OpenVR regression / NVENC streaming / production game) with commands, expected behavior, and a verification log to append to as gates pass. Doc only — actual execution waits on a verification host (Vulkan SDK + NVENC SDK 12.1+ + a real headset).

## Phase 5 — telemetry / dashboard polish (2–3 days)

- Already wired: the `RuntimeMode` selector auto-renders via `SettingsSchema`. No additional dashboard work strictly needed.
- ✅ **Telemetry bridge LANDED 2026-05-21**: bumped bridge ABI to v2 and added `alvr_oxr_report_pacing` (fork `14608d600`, alvr `f4b3cbf4` + `76717ec2`); wired the Rust-side aggregator (alvr `93a4ca62`). Flow:
  `comp_alvr::layer_commit` → `alvr_oxr_report_pacing` → `ServerCoreContext::report_oxr_pacing` → `StatisticsManager::report_oxr_pacing` → `metrics_exporter::try_push(Sample::OxrPacing)`. The aggregator now emits an `oxr_pacing` JSON section with `cpu_us` and `submit_us` distributions whenever a window saw at least one frame; field is omitted otherwise so OpenVR-mode snapshots stay byte-identical. **Verification ceiling**: visible in `metrics_exporter`'s POST output only when a real client connects (StatisticsManager is None pre-connection). Confirmed locally via `cargo check -p alvr_server_core -p alvr_server_openvr -p alvr_server_openxr` + a clean `monado-service.exe` boot at ABI v2.
- Configurable runtime install path. Right now `alvr/xtask/src/build_openxr.rs` hard-codes `openxr/` as the Monado source dir (overridable via `ALVR_MONADO_SOURCE_DIR` env var, but not yet a setting). If we make it a setting, surface it via the same `SettingsSchema` auto-render pattern as `RuntimeMode`.

## Phase 6 — coexistence + cleanup (2–3 days)

- ✅ **Launcher GUI runtime selector LANDED 2026-05-21**. The launcher now reads `session_settings.extra.runtime.variant` for each installation, shows it as a `[SteamVR (OpenVR)]` / `[Monado (OpenXR) — preview]` badge next to the version, and the Edit popup exposes a ComboBox that writes back to `session.json` via `serde_json::Value` manipulation (no schema round-trip — preserves all other fields byte-for-byte). Windows-only (the launcher already gates session-file handling on `cfg!(windows)` per the existing comment). **Verification ceiling**: `cargo check`/`build`/`clippy -p alvr_launcher` all clean, launcher starts without panic on a host with no installations; in-app interaction with a real ALVR installation (read existing variant, switch it, observe re-launched dashboard pick up the change) is not exercised here.
- ✅ **CI: Windows OpenXR-runtime job LANDED 2026-05-22**. `.github/workflows/openxr.yml` already built `XRT_BUILD_DRIVER_ALVR=ON XRT_FEATURE_COMP_ALVR=ON` (`cargo xtask build-openxr-runtime --enable-alvr-driver`) and ran Monado's ctest (~3759 assertions). This session added a `Run OpenXR-mode unit tests` step that runs the two test suites with no other CI coverage: `cargo test -p alvr_launcher` (8 session.json runtime-mode round-trip tests in `alvr/launcher/src/actions.rs`) and `cargo test -p alvr_server_core metrics_exporter` (8 `OxrPacing`/`OxrLayerTypes`/`Battery` aggregator tests in `alvr/server_core/src/metrics_exporter.rs`). Both green locally — `8 passed; 0 failed` each. PR path-gate also expanded to include `alvr/launcher/**` and `alvr/server_core/src/metrics_exporter.rs` so changes to the tested surfaces trigger the workflow on PR. **Confirmed running green on Actions 2026-05-26** (see the three CI fixes below); a third suite, `cargo test -p alvr_server_openxr` (the bridge-pose normalized-quaternion guards), was added to the same step.
  * ✅ **CI FULLY GREEN 2026-05-26** (run for `4b43b856`, 26m; re-confirmed green on `ed546471`). Getting there took three fixes, below. The whole pipeline now passes end-to-end: submodule checkout → Export Vulkan SDK → cdylib build → unit tests (now incl. `cargo test -p alvr_server_openxr`) → Build OpenXR runtime → Monado ctest.
  * **Fix 1 — `MONADO_PAT` secret (private submodule checkout).** `openxr.yml` had been red on every run since 2026-05-22, failing at step 2 `actions/checkout@v6` (every later step skipped) because the `openxr/` submodule points at **private** repo `AndreyVN/monado` and the default `GITHUB_TOKEN` can't read a *different* repo. The checkout already passes `token: ${{ secrets.MONADO_PAT }}` (wired in `1efed4c9`); the missing piece was the secret. **Reading the checkout error: 404** (`Repository not found`) = secret missing/unset; **403** = secret wired but the PAT is underprivileged for `monado` (fine-grained PAT scoped to `alvr` only, missing `Contents: Read-only`, or the secret holds a stale token string after a regenerate). Setup (one-time, repo-admin only):
      1. Create a **fine-grained PAT** at GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens. Resource owner = `AndreyVN`; **Repository access** = only `AndreyVN/monado`; **Permissions** → Repository permissions → **Contents: Read-only**. (A classic PAT with the `repo` scope also works but is broader than needed.) Set an expiry you'll remember to rotate.
      2. In `AndreyVN/alvr` → **Settings → Secrets and variables → Actions → New repository secret**: name it exactly `MONADO_PAT`, paste the token value.
      3. Re-run the latest `openxr.yml` run (Actions tab → the failed run → "Re-run all jobs") or push any commit to `openxr`. The checkout should now resolve the submodule and the build/test/ctest steps will execute for the first time.
    * **Token rotation:** when the PAT expires the checkout will fail again with the same checkout-only symptom — repeat the setup with a fresh token. No in-repo way to detect impending expiry; treat a sudden checkout-only failure as the signal.
  * **Fix 2 — export the Vulkan SDK env (alvr `4b43b856`).** Once checkout passed, the `Build OpenXR runtime` step failed at CMake configure: `cmake/SPIR-V.cmake` does `find_program(GLSLANGVALIDATOR_COMMAND glslangValidator)` and aborts with `glslangValidator required` if absent. `prepare-deps` *does* `choco install vulkan-sdk` (the non-`--ci` path the workflow uses), but **choco sets `VULKAN_SDK`/PATH at the machine level mid-job and GitHub Actions does not surface machine-env changes to later steps** (same reason the cdylib step warned `VULKAN_SDK not set`). Fix: a new `Export Vulkan SDK to environment` step after `Prepare ALVR deps` globs `C:\VulkanSDK\<latest>`, writes `VULKAN_SDK=…` to `$GITHUB_ENV` and `…\Bin` to `$GITHUB_PATH`, and smoke-prints `glslangValidator --version`. **General rule: anything `prepare-deps` choco-installs needs its env re-exported to `$GITHUB_ENV`/`$GITHUB_PATH` in CI.**
  * **Fix 3 — vcpkg binary cache (alvr `ed546471`, perf only).** The `Cache vcpkg installs` step pointed at `build/_thirdparty/vcpkg/installed`, which manifest-mode vcpkg never populates (it installs to `build/openxr-debug/vcpkg_installed`), so the key never hit and every run paid the full ~3.5-min cold port build. Now caches vcpkg's actual binary cache (`~\AppData\Local\vcpkg\archives`, the `Restored N package(s) from …\vcpkg\archives` mechanism), key bumped to `…-v2-` because `actions/cache` won't overwrite the stale empty entry under the old key. First run on the new key still pays the cold cost and saves the archive; subsequent runs restore it.
    * **Diagnosing CI without `gh` (no CLI on this host):** the alvr repo is public, so `Invoke-RestMethod` against `api.github.com/repos/AndreyVN/alvr/actions/...` works unauthenticated for run/job status; read failure text via `commits/{sha}/check-runs` → `check-runs/{id}/annotations` (the `actions/jobs/{id}/logs` download needs auth → ask the user to paste the step log). **Unauthenticated API is 60 req/hr per IP — do NOT poll faster than ~1/min, and never leave a 30s-interval background watcher running or it pins the limit at 0.**
  * **Fix 4 — CI hygiene against two dated deprecations (alvr `f9c5b89e`).** The green run flagged: (a) `actions/cache@v4` runs on Node 20, which Actions forces to Node 24 on **2026-06-02** → bumped to `actions/cache@v5` (v5.0.5, Node 24; same cache backend as v4.2+, so the existing `vcpkg-windows-v2-` archive still restores). (b) `windows-latest` is redirected to `windows-2025`/VS2026 by **2026-06-15** → pinned `runs-on: windows-2022` to lock the proven VS2022 toolchain (MSVC 14.44) the green runs built on, so the swap doesn't silently land on the Monado build. **To validate VS2026 later, deliberately flip to `windows-2025` and re-run** rather than letting the redirect do it unobserved. (`actions/checkout@v6` and `Swatinem/rust-cache@v2` were not flagged and are current.)
- ✅ **CLAUDE.md OpenXR-mode update LANDED 2026-05-22**. Bumped the bridge ABI line from "currently v3" to "currently v5" and expanded the surface description to include hand-tracking (`alvr_oxr_get_hand_skeleton`, v4 — feeds the per-side `alvr_hand` xrt_device) and per-view foveation (`alvr_oxr_get_foveation` / `alvr_oxr_set_foveation`, v5 — gaze-driven cache populated via `ServerCoreEvent::PerViewFoveation` from `PerViewFoveationEmitter`). The "What this is" + "Core verification & run commands" sections already covered `alvr_server_openxr` and the `Settings.extra.runtime` toggle — those didn't need changes.
- ✅ **`ARCHITECTURE.md` OpenXR data-flow LANDED 2026-05-21**. Refreshed the "OpenXR runtime mode (preview)" section: item 2 now lists `alvr_oxr_report_pacing` and notes ABI v2 + the on-load mismatch check; item 3 replaces the stale "stub returning XRT_ERROR_NOT_IMPLEMENTED" with the current `layer_commit` behaviour (projection-layer packing + `SUBMIT_BEGIN/END` bookends + pacing-report call), and warns about the cmake_in/`#define` foot-gun; item 5 mentions the launcher selector; new item 6 walks the pacing-telemetry path through to `metrics_exporter::Sample::OxrPacing`. Also extended the "Telemetry & metrics export" section to enumerate the `Sample` variants and added a "New bridge ABI surface (OpenXR mode)" entry to "Where to extend" with the version-bump + atomic-commit recipe.

## Phase 7 — stretch (1–2 weeks, optional)

* Quad / cylinder / equirect / cube / passthrough layer support — rasterise into a single projection image before encoding.
  * ✅ **Slice 1 LANDED 2026-05-21** — diagnostic only. Bridge ABI bumped to v3 with `alvr_oxr_report_layer_types(frame_id, n_quad, n_cylinder, n_equirect, n_cube, n_passthrough)`. `comp_alvr` now reports a per-frame histogram of the non-projection layers it still drops; `metrics_exporter` aggregates into an `oxr_layer_types` JSON section (per-type totals + frame counter). Reveals what kinds of overlays apps actually submit so Slice 2 (Vulkan quad rasterisation) can be prioritised by observed usage. Commit ladder: fork `ae7e32ec2` / alvr `089e9b34` (Rust ABI v3 + aggregator) / alvr `41f7db24` (submodule bump).
  * ✅ **Slice 2 landed (sub-slices 2a/2b/2c) 2026-05-23**. **Scope in [`PHASE7_SLICE2_SCOPE.md`](PHASE7_SLICE2_SCOPE.md)**. Headline: don't roll a custom quad shader — reuse Monado's existing layer squasher (`comp_high_level_render.h` + `comp_high_level_scratch.h`), which already handles every XRT layer type and exposes its output as an `xrt_image_native` handle that the bridge already consumes.
    * **2a — scope** (alvr `3fcafc82`): captures architecture decisions L1-L5 (reuse squasher, GFX path, R8G8B8A8_SRGB scratch, eager init, projection-only fast path preserved). 156 lines.
    * **2b — lifecycle** (fork `afd818f5f`, alvr `7cf01836`): adds `render_shaders` / `render_resources` / `chl_scratch` fields on `struct comp_alvr_compositor`; new `compositor_init_render_resources` runs after `compositor_init_vulkan` to load SPIR-V + allocate static pipeline resources + zero-init the per-view scratch ring. Destroy chain frees in reverse order before `vkDestroyDevice`. Linked `comp_render` into `comp_alvr` CMake target.
    * **2c — dispatch** (fork `fce89ae82`, alvr `7105970f`): `compositor_init_render_resources` now also calls `chl_scratch_ensure` eagerly (view_count from xdev, RECOMMENDED_VIEW_{WIDTH,HEIGHT} extent). New `compose_via_squasher` wraps the full `chl_frame_state_init` → `render_gfx_init` → `chl_frame_state_gfx_set_views` → `comp_render_gfx_dispatch` → `render_gfx_end` → `vk_cmd_submit_locked` → `vkQueueWaitIdle` → fini sequence, then overwrites `packed[0].image_handles[v]` + `width`/`height` with the scratch image's native handle. `layer_commit` gates the squasher on `(overlay_count > 0 && projection_count > 0)`; projection-only frames keep the existing zero-overhead path; overlay-only frames still produce no submit. Synchronisation today is a deliberate CPU stall (`vkQueueWaitIdle`) — Slice 3.3's encoder body doesn't read the image yet so there's no observable latency cost. A future slice will replace this with a semaphore handoff between squash completion and encoder consumption.
    * **Verification (Gates A + B both green on this Windows host)**: `cargo xtask build-openxr-runtime --enable-alvr-driver` clean (21s incremental); `monado-service.exe` boot log still emits `INFO [comp_alvr_create_system_compositor] ALVR compositor ready` with the eager `chl_scratch_ensure` running on the AMD Radeon 780M iGPU; no VK errors at init.
  * ✅ **Slice 2d landed 2026-05-23 (with a 2c regression discovery)** — new crate `alvr/oxr_overlay_smoke/` is a Vulkan-binding OpenXR client (~350 LOC, openxr 0.21 + ash 0.38) that submits projection + quad layers through `xrEndFrame`. Verified against `monado-service.exe` on the RTX 3090 host (192.168.10.101) with HKLM `ActiveRuntime` pointed at Monado: `Summary: submitted=30 endframe_errors=0 final_state=FOCUSED`. The state machine walked IDLE → READY → SYNCHRONIZED → VISIBLE → FOCUSED across 30 frames, zero VK errors, monado-service stayed alive across the full smoke.
    * **2c regression caught by the smoke**: `compose_via_squasher` called `comp_render_gfx_dispatch`, which hard-asserts on `d->target.initialized` (`compositor/util/comp_render_gfx.c:889-892`). The squasher API has no squash-only mode. Fork `401d04df8` gates the dispatch behind an `#if 0` and returns false early so layer_commit falls back to projection-only submission. The crash would have torn `monado-service.exe` down on the first overlay frame from any production OpenXR app submitting a HUD/menu.
    * **Smoke discovery — state machine deadlock**: gating frame submission on `session_state == SYNCHRONIZED|VISIBLE|FOCUSED` (the obvious thing to do) deadlocks, because `xrEndFrame` itself is the event that drives READY → SYNCHRONIZED via `do_synchronize_state_change` in `oxr_session_frame_end.c`. The smoke client just calls `xrWaitFrame`/`xrBeginFrame`/`xrEndFrame` unconditionally after `xrBeginSession`; the verify checks in `oxr_api_session.c` only require SESSION_RUNNING.
    * **Windows host quirk worth a memory entry**: PS-remoting puts the process in an elevated context, which makes the OpenXR loader IGNORE `XR_RUNTIME_JSON` (see `Error [GENERAL | platform_utils | OpenXR-Loader]` from the smoke bring-up). The only way to drive the smoke from a remoting session is to flip `HKLM\Software\Khronos\OpenXR\1\ActiveRuntime` at the Monado manifest and restore it after. HKCU is checked but the older `openxr_loader.dll` builds don't honour it over HKLM. Already in [[openxr-mode-quirks-windows]] memory.
  * ✅ **Slice 2c.1 LANDED + SMOKE-VERIFIED 2026-05-25** — took path (a): `init_dummy_distortion_target` allocates a throwaway 1x1 `R8G8B8A8_UNORM` `VkImage` + view + `render_gfx_render_pass` + `render_gfx_target_resources` (goto-chain rollback), `compose_via_squasher` attaches it via `chl_frame_state_gfx_set_target` with both views at viewport (0,0,1,1) and identity vertex rots, and `comp_alvr_destroy` tears it down first (reverse-init order). The `#if 0` dispatch wrapper from 2c is now live. The encoder reads the per-view scratch images, not this target, so the distortion output is discarded; cost is a few µs of distortion fragment shader per frame, negligible against the existing `vkQueueWaitIdle` stall. New fields on `struct comp_alvr_compositor` (`dummy_distortion_{image,memory,view,rgrp,rtr}`). Commit ladder: fork `e4b82ed3d` / alvr `156c800f` (submodule bump) / alvr `ae6b078e` (doc).
    * **Smoke-verified on RTX 3090 (192.168.10.101) 2026-05-25**: `oxr_overlay_smoke` (projection + quad each frame, so `layer_commit` exercises the `compose_via_squasher` gate `overlay_count>0 && projection_count>0`) reports `submitted=30 endframe_errors=0 final_state=FOCUSED`. The dummy distortion target dispatches without tripping the `comp_render_gfx_dispatch` `d->target.initialized` assert — i.e. the squasher path 2c.1 unblocked actually runs end-to-end. monado-service alive across all 30 frames, zero VK errors.
    * **The "`xrLocateViews ERROR_RUNTIME_FAILURE` regression" was NOT a regression** — root-caused 2026-05-25 to a **stale `alvr_server_openxr.dll`**. `cargo xtask build-openxr-runtime` deploys `target/debug/alvr_server_openxr.dll` *without rebuilding the Rust crate*, and the on-disk DLL was built 2026-05-22 13:59 — ~9h before fix `1eac1daf` ("AlvrOxrPose::default() returns identity quat", 2026-05-22 22:44). The pre-fix derived `Default` emitted orientation `(0,0,0,0)`; `alvr_hmd_get_tracked_pose` stamped `ORIENTATION_VALID` on it; `oxr_session_locate_views`' `math_quat_ensure_normalized` check then failed with `RUNTIME_FAILURE`. The 2026-05-23 bisect rebuilt monado-service (C) every step but never rebuilt the cdylib, so the bug looked baseline-present. Fix: `cargo build -p alvr_server_openxr` + redeploy → smoke green. **Localized via temporary `fprintf` instrumentation in `oxr_session_locate_views` (reverted; not committed).** Follow-up worth doing: make `build-openxr-runtime` auto-build the cdylib so a stale DLL can't be shipped silently.
  * ⏸ Slice 2e (stretch) — GFX → CS dispatch swap if measurements ever justify it. Pure call-site change; same `comp_render_dispatch_data`, same scratch output.
  * ✅ **Late-join `XrSessionStateChanged` replay LANDED 2026-05-28** (fork `6fd614825` / alvr `efb61c40` submodule bump). `alvr_hub.c::event_poll_loop` caches the latest bridge state and re-pushes it to `g_broadcast_sink` every `ALVR_STATE_KEEPALIVE_INTERVAL_NS` (250 ms) when the event queue is empty. Late-joining OpenXR sessions get the cached state within ~250 ms; OpenXR's state tracker de-dups same-state pushes so the cost is a few wasted broadcast iterations. **Verified on .101 with hello_xr from Khronos OpenXR-SDK 1.1.60 D3D11 binding**: pre-fix, hello_xr's IPC connection collapsed mid-handshake (app exited right after extension enumeration). Post-fix, hello_xr reaches `update_session_state_locked: Started native session, 1 active app session(s)` — the OpenXR session is properly registered and the bridge state reaches it via the keepalive. **Full end-to-end rendering still blocked** by an orthogonal issue: the ALVR streamer's idle-timeout disconnects the Quest before slow-starting OpenXR apps ramp up to `xrEndFrame`. The bridge then fires `CONNECTION_LOST`, the keepalive correctly re-pushes `(false, false)`, and the session stalls. Tracked as the next item below.
  * ⏸ **Idle-disconnect: ROOT CAUSE IDENTIFIED 2026-05-28** via Wireshark capture on `.101` + reading `alvr/client_openxr/src/lib.rs:416`. Found in three layers: (1) wire — tshark on `.101` Ethernet port 9943 captured the disconnect: Quest sends FIN (frame 613, 80.174s) followed 500ms later by RST (frame 616, on server's next KeepAlive over the now half-closed socket). So **Quest's TCP stack actively closes** — not Wi-Fi / NAT / OS timeout. (2) Mirror logs — no client-side `info!()` from any of the 5 disconnect_notif paths appeared. (3) Code — `client_openxr/src/lib.rs:411-419` handles `SessionState::STOPPING` by calling `core_context.pause()`, which sets `lifecycle` to `Idle`. `client_core/src/connection.rs:455` exits its loop when `lifecycle != Resumed` and fires `disconnect_notif.notify_one()` at line 534 — **previously silent (no info!())**. So the chain: Quest's Meta OpenXR runtime emits STOPPING (proximity sensor blip / screen-off / app-focus loss / system event) → alvr.client.dev pause() → lifecycle Idle → control_send_thread exits silently → sockets dropped → wire FIN. **Multi-part fix LANDED 2026-05-28**: (a) alvr `9c67d6f3` tags the previously-silent line-534 disconnect `[control_send_thread exit]: lifecycle off Resumed (alvr_pause)`. (b) alvr `a977eb78` **debounces STOPPING in `client_openxr/src/lib.rs`** — handler decouples spec-mandated `xr_session.end()` (prompt) from `core_context.pause()` (deferred 2 s). If READY arrives within the window, the pending pause is cleared (log: `"STOPPING debounced — kept ALVR connection alive"`); otherwise the deadline expires (log: `"STOPPING debounce expired"`) and behaves as before. (c) alvr `8f8dc881` **fix(graphics): include FFE_RUNTIME binding 2 in the static pipeline layout** — discovered when the rebuilt APK panicked on the Quest with `wgpu error: Validation Error: Shader global ResourceBinding { group: 0, binding: 2 } is not available in the pipeline layout`. The `stream.wgsl` shader unconditionally declares `@binding(2)` for the FFE_RUNTIME path's ffe_rt uniform; WGSL doesn't gate resource declarations by override constants, so wgpu validates the static pipeline's shader-interface against its layout (binding 0+1 only) and panics. Fix: include binding 2 in the main layout with a tiny dummy uniform buffer shared across views; static path's shader code never reads it. (d) APK rebuild + install verified `8f8dc881` removes the panic. **Behavioural verification of (b) — FULL PASS 2026-05-28** with the user actually wearing the headset: session_log captured the smoking-gun line `19:01:39.208 [INFO] Client 7621.client.local.: [Info] STOPPING debounced — kept ALVR connection alive` — a transient STOPPING was caught, READY arrived within 2 s, and the ALVR control TCP stayed alive (no disconnect/reconnect cycle). Sustained STOPPINGs (>2 s) still fall back to the pre-fix behaviour as designed (`STOPPING debounce expired (2s) — pausing ALVR` → wire FIN). One transient win in a 2.5-min test, plus three sustained expirations on the same headset (matches the off-head-detection profile of an imperfectly-worn Quest). Both code paths exercised end-to-end on real hardware. Quest `alvr.client.dev` adb at `192.168.10.201:5555` (USB-debug authorized 2026-05-28).
  * ✅ **OpenXR-mode video reaches the headset 2026-05-28** — first authoritative end-to-end visual confirmation. Smoke client `oxr_overlay_smoke` (which submits unconditionally — see Slice 2d trap #1) drove `comp_alvr → CUDA-import → NVENC → wifi → Quest hevcD → display` to 142 STATS in 60 s with the Quest holding `state=Streaming` the entire window, and the user reported the per-eye foveated checkerboard **visible** in the headset. Confirms the OpenXR-mode streaming pipeline is functional end-to-end on real hardware. ⏸ Real third-party OpenXR apps (hello_xr, AK.exe) still don't render — investigated 2026-05-28. The original `sleep(250ms)` theory was wrong, caused by `stdout` block-buffering on Windows hiding hello_xr's logs. With `setvbuf(stdout, IONBF)` added, the actual picture is the opposite of "stuck": hello_xr reaches `FOCUSED` in <80 ms and `RenderFrame` runs at 72 fps. BUT **all 243 `xrWaitFrame` calls returned `shouldRender=0`**. Per Monado's `should_render()` in `state_trackers/oxr/oxr_session.c:121-128`, `shouldRender` is only `true` when state is `VISIBLE` / `FOCUSED` / `STOPPING`; for `SYNCHRONIZED` it's `false` (per OpenXR spec — apps SHOULD NOT render in SYNCHRONIZED). Trace from a single hello_xr launch shows session state going `UNKNOWN → IDLE → READY → SYNCHRONIZED → VISIBLE → FOCUSED → VISIBLE → SYNCHRONIZED` all within 80 ms, then **stuck in SYNCHRONIZED ever after**. Within 65 ms of reaching FOCUSED, the Meta runtime's next STOPPING blip (off-head transient) pushed `visible=false` into the bridge, my `alvr_hub` keepalive broadcast `false` to all sessions, and hello_xr's session correctly dropped back to SYNCHRONIZED. From then on `shouldRender=0`, hello_xr skips `RenderLayer`, `xrEndFrame` is called with empty layers, `comp_alvr`'s `layer_commit` gates with `packed_count=0` so `alvr_oxr_submit_layers` is never invoked, STATS=0. **Smoke works because its connection happened to stay in `state=Streaming` for the full 60 s** — `oxr_overlay_smoke`'s own `should_render` check passed continuously. Real fix is to debounce the SERVER-side bridge state — currently the keepalive broadcasts `visible=false` immediately on every `ConnectionLost`, churning hello_xr's session state. Could give the bridge a grace window (~2-5 s) before flipping `visible=false`, similar in spirit to the client-side STOPPING debounce. Then a brief Quest cycle wouldn't kick real OpenXR apps out of FOCUSED.

  * ✅ **Bridge ConnectionLost debounce LANDED 2026-05-28** (fork `a0befd71a` / alvr `32290689` submodule bump). `alvr_hub.c::event_poll_loop` now defers the `visible=false / focused=false` transition by `ALVR_BRIDGE_CONNECT_DEBOUNCE_NS` (3 s) when ConnectionLost fires; if a fresh ClientConnected arrives within the window, the pending transition is cancelled entirely. Mirror of the client-side STOPPING debounce (`a977eb78`) on the opposite end of the cycle. **Verified end-to-end with hello_xr on .101 + Quest 3**: `shouldRender=1` count climbed from 40 to 111 over a 60-s window with multiple Quest disconnect cycles in flight — every cycle of those would, pre-fix, have dropped hello_xr to SYNCHRONIZED and `shouldRender=0` within 65 ms.
  * ✅ **Projection-only encoder fix LANDED 2026-05-28** (fork `e0e48a300` / alvr `c5f6c383`). Diagnostic counters in `comp_alvr_layer_commit` (fork `1f63c1a0b`) revealed that hello_xr's `pack_projection_layer` reads back `handle=0x0 size=0` on every view (root cause still TBD inside Monado's IPC swapchain plumbing — likely related to how `xrt_image_native.handle/size` survive the multi-system layer handover). Smoke worked anyway because projection+quad triggers `compose_via_squasher`, which OVERWRITES `pack[0].image_handles/sizes` with valid native scratch handles before the encoder sees them. The fix: drop the `skipped_total > 0` gate so the squasher runs on projection-only frames too, publishing valid scratch handles regardless of overlay count. **Verified on .101 + Quest 3 + hello_xr -g D3D11**: `submit_calls=1199 submit_ok=1199 submit_failed=0`, STATS=107 in ~30 s, then continued past STATS=197 across a multi-minute window with `submit_ok=3153` cumulative. **User confirmed hello_xr's standard sample scene (a few colored cubes representing hand/controller pose anchors) visible in the headset** — first authoritative end-to-end visual confirmation that a real third-party OpenXR app renders through OpenXR mode. Cost: squasher dispatch per frame even on pure-projection apps (vkQueueWaitIdle stall + dummy distortion target — a couple of ms, acceptable for the first real-app verification). Optimisation paths for later: (a) fix the underlying pack_projection_layer `handle=0` issue so the squasher overwrite isn't needed, or (b) replace the CPU stall with the semaphore handoff Slice 2c already notes. Diagnostic counters stay in place as durable observability.
  * ⏸ **AK.exe retest 2026-05-28 (post-debounce + post-squasher-always-on): pipeline now reaches the encoder, headset still black** — UE5 OpenXR sample reaches `xrCreateInstance` → `Started native session` → engine init → `Resizing VR buffer to 4288 by 2240` (UE5's stereo atlas) → `xrEndFrame` loop. `comp_alvr_layer_commit` counters: **`submit_calls=13498 submit_ok=13498 submit_failed=0`** sustained over the working window (≈3.5 min at 60-ish fps). Quest's client log shows `[STATS]` / `[OXR STATS]` / `[GRAPH]` flowing throughout at decoder rate. **Headset showed black** the entire time. Two distinct things still wrong: (1) **Black despite valid e2e plumbing.** Initial hypothesis: UE5 uses a single shared stereo-atlas swapchain so `compose_via_squasher`'s sampling doesn't honour the per-view rect. **Extended pack_projection_layer diag (fork `d26ce8b87`) DISPROVED this** — captured live on .101: `view 0 norm_rect={0.0000,0.0000,0.5000,1.0000}`, `view 1 norm_rect={0.5000,0.0000,0.5000,1.0000}`, `array_index=0`, `atlas=1` (same `sc[0]==sc[1]` pointer for both views). UE5 does use the shared-atlas layout, but Monado's `do_projection_layer` (`comp_render_gfx.c:391-409`) feeds `vd->sub.norm_rect` through `set_post_transform_rect` into the descriptor set, and `layer_projection.vert:48` applies it: `uv = fma(in_uv, ubo.post_transform.zw, ubo.post_transform.xy)` — so left view samples `(0..0.5, 0..1)` and right view samples `(0.5..1, 0..1)` correctly. The atlas path is already handled. **ROOT CAUSE ISOLATED 2026-05-28** via the FFR-off retest. The encoder is configured at `openvr_config.eye_resolution * 2 = 4288×2240` (AK's stereo width), **which exceeds H.264's max frame width of 4096**. Caught in `session_log.txt`: `[WARNING] OpenXR mode: NVENC encoder unavailable; no video will stream. Encoder config: codec=0 4288x2240 @ 72Hz, 30000000 bps. Reason: NvEncoder::CreateEncoder : m_nvenc.nvEncInitializeEncoder(...) returned error 8` (= `NV_ENC_ERR_UNSUPPORTED_PARAM`). **The encoder NEVER initialised; no video ever shipped to the headset.** The `submit_ok=13498` in the earlier retest is a separate observability bug: `alvr_oxr_submit_layers` returns OK regardless of encoder availability, so the counter rises while the encoder is silent. The Quest's `[STATS]` / `[OXR STATS]` / `[GRAPH]` lines that *appeared* to be decoder activity were the control-channel heartbeat (hand skeleton, reference-space events, session telemetry) — not actual decoded frames. Three independent issues compounded into one "black screen": (1) FFR-off path's `chl_scratch.native_images[idx].size == 0` hits the encoder's `handle==0 || size==0` import gate (`submit_failed=100%`); (2) FFR-on with H.264 fails `nvEncInitializeEncoder` at >4096 width; (3) FFR-on with HEVC (max 8192) still produced `submit_failed=100%` with no NVENC log, suggesting the encoder's expected input dims (configured from `openvr_config.eye_resolution`) don't match the FFR scratch dims actually passed in `pack->width/height` — i.e. an init-vs-submit resolution mismatch. **Hello_xr worked because its per-eye width was ≤2048, keeping stereo encoder width ≤4096 inside H.264's limit**, not because anything in the squasher specifically supports per-eye chains better than atlas chains. **Fix path is encoder-side, not squasher-side.** Three changes line up into one slice: (a) encoder should be configured at the actual encode resolution (FFR scratch when FFR on, eye scratch otherwise) rather than `openvr_config.eye_resolution`; (b) `alvr_oxr_submit_layers` should propagate encoder-unavailable as a non-OK so it's visible in counters and at the bridge boundary; (c) populate `comp_scratch_single_images::native_images[idx].size` (probably already known at allocation time, just not stored) so the FFR-off path becomes usable.

  * ✅ **Encoder resolution fix LANDED 2026-05-28** (alvr `c591ff92`). `foveation_compress_vars_from_config` now gates on `settings().video.foveated_encoding` (the user-persistent intent) rather than `oc.enable_foveated_encoding` (which folds in the client's streaming_caps at connection time and races with comp_alvr's boot-time decision). Both consumers — `alvr_oxr_get_foveation_vars` (called by comp_alvr at boot) and the encoder's `build_config` (called at ClientConnected) — now produce identical Some/None outcomes regardless of timing. A one-line INFO log records the resolved config once per encoder start so two-source mismatches are visible in the mirror. **Verified on .101 + Quest 3 + AK.exe**: `OpenXR encoder build_config: ffr=true per_eye=1280x1184 encoder_frame=2560x1184 eye_resolution=2144x2240 codec=0`. NVENC initialised cleanly, `submit_ok=N/N submit_failed=0`, Quest decoder saturated (real-content frames flowing — frames at all bitrate is the marker; saturation only happens when the bitstream isn't empty). H.264 limit cleared. Encoder + comp_alvr in lockstep on dimensions. **Headset still showed black** — remaining bug has moved to a different layer (client decode/de-foveation, OpenXR layer presentation, or UE5 PSO-warmup latency). Encoder-resolution issue itself is fixed; (b) (`alvr_oxr_submit_layers` propagating encoder-unavailable) and (c) (`chl_scratch` size population) remain as follow-ups; the FFR-on path doesn't need either today.

  * ✅ **Client de-foveation pipeline verified healthy on the same FFR-on config 2026-05-29**. Re-ran hello_xr against the new encoder config (`ffr=true per_eye=1280x1184 encoder_frame=2560x1184 eye_resolution=2144x2240 codec=0` — same as AK), `submit_ok=599/599`, decoder saturation warnings (same signature as AK). User confirmed hello_xr's colored cubes visible in the headset. That rules out the de-foveation pipeline, OpenXR projection-layer composition, MediaCodec decode path, and the FFE shader-constant math as the cause of AK's black — all of them just rendered hello_xr's content correctly under identical server-side dimensions.

  * ⏸ **PSO-warmup hypothesis DISPROVEN + actual root cause pinned 2026-05-29.** Long-run AK test (2.5 min after launch, `Engine is initialized. Leaving FEngineLoop::Init()` printed, only 42 PSOs total compiled within 30s of launch, UE5 fully past warmup): headset still black. Added a per-window encoder NAL bitstream-size diagnostic (alvr `c80ed7f1`) and immediately caught the signature: **`OpenXR encoder bitstream diag: packets=6600 idrs=5 total_bytes=252321 avg_bytes_per_packet=38.2`**. 38 bytes per packet is ~1300× smaller than what a 30 Mbps stream at 72fps from real content would produce. This is what H.264 outputs for uniform-black input (motion-estimation floor + slice headers). **The encoder is consuming BLACK input — AK's actual rendered content is not reaching the encoder despite the squasher's `submit_ok` counting frames through.** Combined with: (a) hello_xr rendering correctly through the same FFR-on / encoder / decode / de-foveation pipeline; (b) AK and hello_xr both confirmed to be running their normal frame loops; (c) the encoder bitstream-size delta — the bug is **upstream of the encoder, in the squasher / D3D11→Vulkan swapchain-sampling path for shared-stereo-atlas swapchains specifically**. Per-eye chains work (hello_xr). Shared atlases (UE5) produce black scratch despite `do_projection_layer`'s sub-image / norm_rect math being correct on paper. Suspected mechanisms in order: D3D11→Vulkan image-import sync (multi_compositor's `wait_thread` may be no-op'ing AK's sync_handle), descriptor-set sampling reading from an uncommitted image layout for the second view that shares the chain with the first, or a Monado-side bug where shared-chain references are acquired only once. Concrete next experiments: instrument `compose_via_squasher` to vkCmdCopyImage one tile of AK's source swapchain to a host-readable buffer and dump the bytes (definitive: empty = sampling source is black; non-empty = sampling but not writing to scratch); or temporarily replace `comp_render_gfx_dispatch` with a direct `vkCmdBlitImage` per-view-region from AK's swapchain to scratch to see if the bypass renders. Both are Monado-side.

  * ✅ **Source-tile dump experiment LANDED + ANSWERED 2026-05-29** (fork `bdf9da388`, alvr `63624a1a`). Added `dump_view0_source_tile` in `comp_alvr.c`: allocates a host-visible+coherent 32x32 RGBA8 staging buffer via `vk_buffer_init`, barriers view 0's swapchain image UNDEFINED→TRANSFER_SRC_OPTIMAL, copies a centre tile via `vkCmdCopyImageToBuffer`, barriers back to SHADER_READ_ONLY_OPTIMAL so the squasher's gfx dispatch still finds its expected layout. Maps + counts non-zero bytes + hex-dumps first 16. Fires for first 5 projection-layer packs after stream start; gated on `c->ffr.active`. Implementation gotcha caught: **`vk_buffer_init` returns `bool`, not `VkResult`** — the first deploy treated `true` (= 1, which prints as `VK_NOT_READY` via `vk_result_string`) as failure and logged "VK_NOT_READY" five times. Fixed. **Result on .101 + Quest 3 + AK.exe**: every byte of every sampled tile is zero. `non_zero=0/4096` for all 5 frames; `first_16=00000000 00000000 00000000 00000000`. Combined with the encoder's 38 bytes/packet (alvr `c80ed7f1`), this **rules out the squasher entirely** and pins the bug to the source image being literally empty at sample time. AK is rendering its UE5 scene (Engine init done, RenderFrame loop running on the host side), so Monado must be sampling AK's swapchain *before* AK's D3D11 work has actually landed in the imported Vulkan image. That points squarely at **`multi_compositor`'s wait_thread not actually waiting on AK's GPU-completion sync** — `xrt_comp_import_fence` likely failing for AK's specific NT-shared-D3D11 fence type, falling through to the "assume the app side compositor waited" path (`comp_multi_compositor.c:807-815`), and comp_alvr proceeding to sample uninitialised content. Per-eye chains (hello_xr) appear to work by luck of finishing the small render before Monado samples; UE5's heavier per-frame work loses the race. Next experiments to nail down the sync side: instrument `multi_compositor_layer_commit` to log whether `xrt_comp_import_fence` succeeded or fell through to the no-wait branch for AK; if it fell through, log the `sync_handle` validity + the import error, then choose between fixing the import (most correct) or adding an explicit `vkDeviceWaitIdle` / barrier before sampling in comp_alvr (heavy hammer but unblocks UE5 immediately).

  * ⏸ **multi_compositor instrumentation WIP — open mystery 2026-05-29** (fork `c923f9ee6`, alvr `aaa1df00`). Added `alvr_fence_diag` to `multi_compositor_layer_commit` (records `sync_handle_valid`, `import_attempted`, `import_result`, branch=WAIT_THREAD/ASSUME_APP_WAITED) and `alvr_sem_diag` to `multi_compositor_layer_commit_with_semaphore` (records xcsem nullness + value). Throttled first 5 + every 300. **Both fire zero times when AK runs**, despite the same run showing `src_image_dump` firing 5 times + `comp_alvr_layer_commit` cumulative counters incrementing — i.e. `comp_alvr_layer_commit` IS being called, so frames ARE reaching the downstream system compositor, but neither multi_compositor entry point sees them. Tried U_LOG_W instead of U_LOG_I (rules out log-level filtering). The two known IPC handlers — `ipc_handle_compositor_layer_sync` and `ipc_handle_compositor_layer_sync_with_semaphore` in `ipc_server_handler.c` — call `xrt_comp_layer_commit{,_with_semaphore}` on `ics->xc`, which is assigned at `ipc_handle_session_create` from `xrt_system_create_session`'s `xcn` (the per-app native compositor). Open question for next session: is `xrt_system_create_session` returning a compositor that isn't the multi_compositor here? Or are AK's frames reaching `comp_alvr` via a different code path (overlay layer, multi_system_compositor's render-thread sweep)? Concrete next steps recorded in the fork commit: (a) drop a U_LOG_W unconditionally at the very top of `multi_compositor_layer_commit` to definitively confirm whether the function is reached at all; (b) if not, instrument `xrt_system_create_session` to log what compositor type it hands back for AK; (c) or instrument `ipc_handle_compositor_layer_sync` directly to see whether AK's RPCs reach that handler. The encoder-resolution fix + bitstream-size diag + source-tile dump from earlier in the session all stay landed and unrelated to this open question. **The encoder-resolution fix (`c591ff92`) is unrelated to this black; it stays landed as a necessary precondition that lets the bitstream-size diag be readable at all.** (2) **Bridge-flagged `Client disconnected` mid-stream → `submit_failed` climbs monotonically** (164 → 6592 over the next 90 s) while the Quest's `[STATS]` traffic *continued unbroken* in `session_log.txt`. So the bridge thinks the streaming client is gone, but the streaming socket is still live — separate from the v8 ConnectionLost debounce that landed earlier today. Suspect a second event path in the bridge or in `alvr_server_core` that flips the encoder bridge to a failure mode without taking the streaming socket down with it. Worth instrumenting on the next bring-up pass.

  * ✅ **AK array_size PROVEN = 1 (atlas, NOT multiview) 2026-05-31 — settles the NEXT_STEPS open question.** (fork `cd7c0874f`, alvr `8e668312`.) Added a swapchain-create probe in `client_d3d12_create_swapchain` (`comp_d3d12_client.cpp`) that records the app-requested `array_size`/`width`/`height`/`format`/`bits`. **Crucial detail: UE5 swallows `openxr_monado.dll` stderr**, so the first cut (`U_LOG_E`, fork `2a568cf1c`) produced nothing in `ak.out` — converted to a **file-write** gated on the `ALVR_DIAG_FILE` env var (append), which AK can't intercept (fork `cd7c0874f`). Ran on .101 + monado-service via the non-elevated `ALVR_MONADO_SERVICE` schtask + AK via `alvr_diag_ak` (`run_ak.cmd` now sets `ALVR_DIAG_FILE=C:\alvr\test-openxr\ak_arraysize.diag`). Result — AK creates exactly two swapchains, both atlas:
    ```
    ALVR_D3D12_SWAPCHAIN_CREATE array_size=1 width=4288 height=2240 format=0x5b bits=0x61  (color, DXGI B8G8R8A8_UNORM_SRGB)
    ALVR_D3D12_SWAPCHAIN_CREATE array_size=1 width=4288 height=2240 format=0x14 bits=0x62  (depth, D32_FLOAT)
    ```
    **AK requests `array_size=1` (a 4288-wide side-by-side atlas), NEVER `array_size=2` (a multiview texture-array).** So the open question from the entry below ("does AK *request* an array swapchain and Monado reject it, or does Monado never advertise array support so UE5 falls back?") resolves to: **UE5 picks the atlas on its own — it never even asks Monado for an array swapchain.**
    - **Fix direction is now pinned (and it is NOT the import path):** since AK won't request `array_size=2`, "support array-swapchain import in `comp_d3d12_client`/`comp_alvr`" wouldn't help — UE5 wouldn't use it. The real lever is **why UE5's OpenXR plugin chooses single-pass-atlas (`array_size=1`) over multiview against Monado**, when it chose a consistent (working) stereo path against SteamVR. UE5's OpenXR RHI decides atlas-vs-array from what the runtime advertises. **NEXT: compare what Monado vs SteamVR advertise to UE5 that drives this choice** — `XR_VARJO_quad_views`/view-config enumeration, `XR_FB_foveation`, and most likely **whether the runtime exposes `XR_KHR_composition_layer_depth` / a recommended swapchain layout** that UE5's `FOpenXRRenderBridge`/`FOpenXRSwapchain` uses to pick `bSinglePass`/array vs atlas. Check `openxr/src/xrt/state_trackers/oxr` extension advertisement + `xrEnumerateViewConfigurationViews` (does Monado report `XR_VIEW_CONFIGURATION_TYPE_PRIMARY_STEREO` with the recommended array size UE5 expects for multiview?). Whatever flips UE5 to its multiview path makes the `VIEW_ID` VS/PS shaders link → AK renders.
    - **DURABLE: client-side diags in `openxr_monado.dll` MUST be file-writes (env `ALVR_DIAG_FILE`), never `U_LOG_*`/stderr — UE5/AK eats stderr.** The probe + run scripts are persisted: `run_ak.cmd` sets `ALVR_DIAG_FILE`, backup at `run_ak.cmd.pre-arraydiag`; the diag lands at `C:\alvr\test-openxr\ak_arraysize.diag`. **DURABLE: the `array_size`-probe run is fully unattended-driveable** — it only needs AK to reach `xrCreateSwapchain` (no headset-worn required), via `Start-ScheduledTask ALVR_MONADO_SERVICE` then `Start-ScheduledTask alvr_diag_ak`, then read the diag file (appears ~15s after AK launch).

  * 🎯 **AK-BLACK EXACT ROOT CAUSE NAILED 2026-05-31 (D3D12 debug layer) — UE5 instanced-stereo VS/PS `VIEW_ID` shader-linkage mismatch. Depth was a red herring.** Ran AK with the D3D12 debug layer (`AK.exe ... -d3ddebug -gpuvalidation`, Graphics Tools installed on .101) against Monado. **All 156 PSO failures (156/156) are one error:**
    > `[D3DDebug] ID3D12Device::CreateGraphicsPipelineState: Vertex Shader - Pixel Shader linkage error: Signatures between stages are incompatible. The input stage requires Semantic / Index(VIEW_ID, 0) as input, but it is not provided by the output stage.`
    The PIXEL shader requires `VIEW_ID` (`SV_ViewID` — the D3D12 view-instancing / single-pass-stereo / multiview semantic) but the VERTEX shader doesn't output it → linkage fail → `E_INVALIDARG`. The dumped state shows `DepthEnable = 0`, so the earlier `DSVFormat=0x0` depth hypothesis is **wrong** — it's purely the multiview semantic mismatch.
    - **Mechanism:** AK's cooked UE5 build has **Instanced Stereo** enabled, so its pixel shaders are compiled for view-instanced single-pass stereo (expect `VIEW_ID`, which routes to texture-array slices). In OpenXR-Monado mode AK renders to a **side-by-side atlas** (arraySize=1, the 4288 texture) instead of a 2-layer texture array, so the vertex-shader path doesn't emit `VIEW_ID` → every scene PSO fails to link → nothing draws → black (headset AND mirror).
    - **Monado-specific** (corroborated): AK renders visibly via SteamVR (2026-05-30 truth table) ⇒ 0 fatal PSO failures there ⇒ via SteamVR UE5 uses a consistent (array/multiview) stereo path. Live SteamVR re-run this session couldn't bring vrserver up (ALVR's virtual HMD needs a connected Quest to activate), so the baseline rests on the prior truth-table evidence.
    - **LEADING FIX (elegant — fixes AK *and* obsoletes the per-eye-split for UE5): make the ALVR-OpenXR runtime support TEXTURE-ARRAY (multiview) swapchains (`arraySize=2`).** Then UE5 uses its native instanced-stereo path consistently (VS+PS both multiview, routing to array slices), the shaders link, AK renders. BONUS: each array layer is per-eye (≤2144 wide) → no 4288 atlas → the >4096 import bug never arises and the per-eye-split is unnecessary for array-using apps. **Open question to confirm first:** does AK *request* an array swapchain and Monado reject it (forcing the atlas), or does Monado never advertise array support so UE5 falls back? Check Monado's `xrCreateSwapchain` array handling + `comp_alvr`/squasher/encoder array-layer sampling, and whether UE5's OpenXR plugin picks atlas because of something Monado advertises (view config / `arraySize` caps). The squasher's `do_projection_layer` already handles `image->array_size>1`, so the array path may be close.
    - **Artifacts on .101:** `ak_dbg.viewid.out` (full d3ddebug log). **DURABLE: run AK with `-d3ddebug -gpuvalidation` → `ak_dbg.out` `LogD3D12RHI: Error: [D3DDebug]` lines give the exact CreateGraphicsPipelineState failure reason; the 156 failures are ALL the `VIEW_ID` VS/PS linkage mismatch.**

  * 🎯 **AK-BLACK ROOT CAUSE FOUND 2026-05-31 — AK's UE5 renderer fails to create 156 graphics PSOs (E_INVALIDARG) in OpenXR-Monado mode; it renders BLACK to its OWN PC mirror window. Nothing in ALVR/Monado "eats" AK's content — there is no content.** This redirects the entire multi-day "AK black" investigation: the bug was never in the squasher / encoder / import / split / sync — AK simply produces a black frame.
    - **Ground truth — AK's PC mirror window is fully black.** Captured AK's own UE5 spectator/mirror window via `PrintWindow(PW_RENDERFULLCONTENT)` (interactive scheduled task `alvr_capwin`): solid black, 12.8 KB PNG. The mirror is rendered by UE5 directly to the desktop, completely independent of Monado / the split / streaming / the headset. **AK is rendering black frames itself.**
    - **Cause: 156 graphics-PSO creation failures, all `error 80070057` (E_INVALIDARG).** `ak.out`: `LogD3D12RHI: Error: Failed to create pipeline state ... error 80070057` ×156, across 24 distinct vertex shaders, plus `LogRHI: Error: Failed to create graphics pipeline`. With its whole shader set failing to compile pipelines, UE5 can't draw the scene → black mirror + black atlas. The dumped descs show `NumRenderTargets=1, RTVFormats[0]=0x5B (B8G8R8A8_SRGB), DSVFormat=0x0 (no depth), SampleDesc={1,0}` — a D3D12 PSO with depth state but `DSVFormat=UNKNOWN` fails with exactly `E_INVALIDARG`, so the depth-target/format setup is the prime suspect. Also `LogHMD: Warning: Requesting 10 bit swapchain, but not supported: fall back to 8bpc`.
    - **NOT the split** (proven two ways): (a) the split is verified correct via hello_xr (real content end-to-end); (b) PSO-failure count is **identical (156) with `D3D12_COMPOSITOR_STEREO_SPLIT=1` and `=0`** — the split doesn't touch AK's own-device PSO creation.
    - **OpenXR-Monado-specific** (not AK-inherent): AK renders **visibly via SteamVR** (2026-05-30 truth table) → its mirror shows content there → the 156 PSO failures only happen against the Monado OpenXR runtime. So something Monado's OpenXR D3D12 binding advertises/sets up makes UE5 build invalid PSOs (most likely the depth swapchain handling / advertised swapchain formats / `XR_KHR_composition_layer_depth` support → `DSVFormat=0x0`).
    - **NEXT (new direction — chase the PSO failure, not the pipeline):** (1) confirm Monado-specific by counting `ak.out` PSO failures under SteamVR (`run_ak_steamvr.cmd` / `alvr_steamvr_ak`) — expect 0. (2) Get the *exact* invalid PSO field: run AK with the D3D12 debug layer (UE5 `-d3ddebug` or `d3d12.EnableD3D12DebugLayer`) so the D3D12 runtime spells out *why* `CreateGraphicsPipelineState` returns E_INVALIDARG (likely "depth enabled with DSVFormat UNKNOWN"). (3) Compare what Monado advertises to AK vs what SteamVR does: `xrEnumerateSwapchainFormats` (color+depth), `xrGetD3D12GraphicsRequirementsKHR` (adapter LUID), and whether `XR_KHR_composition_layer_depth` is exposed. (4) If it's the depth format: see how UE5's OpenXR D3D12 RHI derives its DSVFormat from the runtime and what Monado returns. **DURABLE: AK's mirror window is the ground-truth "is UE5 rendering" check — `PrintWindow(PW_RENDERFULLCONTENT)` on AK's MainWindowHandle via an interactive schtask (`cap_window.ps1` pattern). Black mirror = AK renders black, full stop.** **DURABLE: `ak.out` `Failed to create pipeline state ... 80070057` count is the AK-render-health signal — 156 = broken, 0 = healthy.**

  * ✅ **PER-EYE-SPLIT VERIFIED CORRECT ON .101 2026-05-31 — and AK's black is PROVEN to be AK rendering black, NOT a split defect.** Ran the split end-to-end on .101 (RTX 3090 + Quest). Results, in order of what they prove:
    - **The D3D12 app→comp COPY mechanism works** (kills the months-old "copy doesn't populate" / "D3D12 import fundamentally broken" confusion — it was always a `src_image_dump` measurement artifact). `hello_xr -g D3D12` + `D3D12_COMPOSITOR_FORCE_COPY=1` → `squash_view_dump non_zero=4096/4096 first_16=779797ff` (hello_xr's real grey-blue cube content) for both views. The full copy populates a ≤4096 comp image and the squasher samples it correctly.
    - **The per-eye-split REGION copy + import + remap works** (the decisive test). Forced hello_xr's advertised per-view width to 4100 (`ALVR_FORCE_VIEW_WIDTH=4100`, new env-gated diag in `compositor_init_sys_info`, overrides only the advertised recommended width, scratch untouched) so hello_xr creates a >4096 swapchain and exercises the split with KNOWN content + READABLE logs (hello_xr doesn't swallow openxr_monado.dll logs the way UE5 does). Result: `Per-eye-split: 4100x2240 atlas -> two 2050x2240 per-eye natives` fires; `pack_projection_layer atlas=0` with two distinct natives + `norm_rect={0,0,1,1}`; **`squash_view_dump non_zero=4096/4096 first_16=779797ff`** — the region-copied per-eye natives carry hello_xr's real content end-to-end. (Note: hello_xr's per-eye swapchain has a single view at center_x=0.5 → the remap picks the RIGHT native for both, so this proves the right-half region copy `[2050,4100)`; the left-half `[0,2050)` is the trivial offset-0 case.)
    - **AK through the split: still steady clear-color** (`squash_view_dump 1024/4096 first_16=000000ff` = the `(0,0,0,255)` render-pass clear, across ~3000 packs — early, middle, AND late bursts; the dump `frame=N` is the cumulative dumped-frame counter, only bumped during bursts at packs 1-5/1500-1504/3000-3004, so frame=15 ⇒ AK reached ~3000 packs). `pack_projection_layer` confirms the split is active for AK too (`atlas=0`, two natives `sc[0]!=sc[1]`, `norm_rect={0,0,1,1}`). Since the identical region-copy+import+remap path demonstrably carries hello_xr's real content, **AK's black means AK is rendering black into its atlas** — the split is innocent. This is consistent with AK's chronic Quest-instability (Client connect→`ReadFile failed`→disconnect→reconnect cycling during the run); AK submitted projection layers steadily but the content was black (likely invalid view poses / no stable tracking in this OpenXR-mode session → UE5 renders black). AK renders REAL content via SteamVR (proven 2026-05-30), so it's not AK-inherent — it's the OpenXR-mode session/tracking/disconnect path.
    - **CONCLUSION: the >4096 atlas-import root cause is FIXED by the per-eye-split (proven).** The remaining AK black is the SEPARATE, pre-existing OpenXR-mode issue where AK renders black content (Quest disconnect/instability / view-pose / tracking) — the same chronic disconnect tracked elsewhere in this doc. Next: get AK to render REAL content (stabilise the Quest session / fix the tracking-in-OpenXR-mode path / test with the Quest worn for a sustained FOCUSED window), then the split will deliver it. Also: the split is correct and could be promoted from opt-in (`D3D12_COMPOSITOR_STEREO_SPLIT`) to default-on for `width>4096` (it's a strict no-op for ≤4096 and replaces a guaranteed-black import with a correct one) — deferred until the whole AK path is visible end-to-end.
    - **Diag added (fork, will commit):** `ALVR_FORCE_VIEW_WIDTH` num-option in `comp_alvr.c::compositor_init_sys_info` (env-gated, default 0) — forces the advertised recommended per-view width so a small app can drive the >4096 split path. The width-probe harness from 2026-05-30 used a hard-coded `rec_w` override; this is the clean env-gated version. `.101` run scripts: `run_ak.cmd` carries `D3D12_COMPOSITOR_STEREO_SPLIT=1`; backups at `.pre-split`/`.pre-forcecopy`.

  * 🛠️ **PER-EYE-SPLIT IMPLEMENTED 2026-05-31 — the actual >4096 fix, opt-in, awaiting .101 verification.** (fork `dacac18a6`, alvr `9b22ee05`.) Built on the known root cause (single D3D12 shared texture >4096 wide imports black on NVIDIA): when a D3D12 swapchain is wider than 4096 — UE5's 4288 stereo atlas — split it into **two 2144-wide per-eye native swapchains** before the D3D12→Vulkan import, which is the EXACT shape hello_xr proves works. Gated behind `D3D12_COMPOSITOR_STEREO_SPLIT=1` (default off, no behavior change until enabled).
    - **How it works (all in `comp/client/comp_d3d12_client.cpp` + `aux/d3d/d3d_d3d12_helpers.cpp`, NO comp_alvr changes):** `client_d3d12_create_swapchain` detects `width > 4096 && array_size==1 && even width`, allocates left+right comp images (`SAMPLED` bits → `COMMON` state, the cross-API-handoff-correct state matching the existing copy workaround), builds one `createCommandListImageCopyStereoSplit` command list per image (region-copy atlas `[0,2144)`→left, `[2144,4288)`→right), and imports both halves as `sc->xsc` (left) + `sc->xsc_r` (right). `acquire_image`/`wait_image`/`release_image_split` drive both natives in lockstep (same image_count → ring stays in sync; a warn fires if indices ever diverge). The copy runs on `c->app_queue` (the app's own render queue) at release, so it's correctly ordered after the app's render. `client_d3d12_compositor_layer_projection` remaps each view to its per-eye native by `norm_rect` half-centre and rewrites the rect to `{0,0,1,1}`; `image_index` is untouched (lockstep). **Depth handling:** D3D12 forbids partial depth copies and ALVR doesn't use depth, so a split `projection_depth` layer is resubmitted as a plain `projection` layer (`d.proj.v` aliases `d.depth.v` at the same union offset — verified).
    - **Resource-state correctness (checked, since it can't be debug-layer-verified on the AMD host):** the copy SOURCE `data->images[i]` is created by `allocateSharedImages(xinfo)` in `appResourceState` (RENDER_TARGET for the color atlas) and never touched by the app (the app renders the re-imported `app_images[i]`), so it stays in RENDER_TARGET → copy barrier `RENDER_TARGET→COPY_SOURCE→RENDER_TARGET` is right. The DEST comp images are `SAMPLED`-bits → `d3d_convert_usage_bits_to_d3d12_app_resource_state` returns `COMMON` → copy barrier `COMMON→COPY_DEST→COMMON` is right and leaves them COMMON for Vulkan. `data->images[i]` aliases `app_images[i]` (both `OpenSharedHandle` of the same committed shared resource), so the copy on the serial app_queue reads the app's writes — same coherency the existing non-copy path already relies on.
    - **TEST on .101 + Quest + AK:** deploy `openxr_monado.dll` (21:59:53) + `monado-service.exe` (21:59:57) to `C:\alvr\test-openxr\`, set **`D3D12_COMPOSITOR_STEREO_SPLIT=1` in AK's launch env** (it runs in the app's process — same place `D3D12_COMPOSITOR_FORCE_COPY` goes), run AK, read `squash_view_dump` + the encoder bitstream rate. Expected if it works: both views `non_zero` high, encoder bytes/packet 500+ (real), AK visible in headset. **The split rides on the same app→comp copy the FORCE_COPY isolation tests** — if AK is still black, run the hello_xr FORCE_COPY isolation FIRST to tell copy-broken (fix the copy) from split-logic-broken (fix the remap). The two share `createCommandListImageCopy*`.
    - **VERIFICATION CEILING:** Gate A only (clean build, both binaries relinked together 21:59). Behaviour is hardware-gated to the RTX 3090 + a headset; the AMD build host has no D3D12↔Vulkan layout divergence to reproduce the bug and no Quest. This is ~320 lines of blind interop code — expect at least one iteration on .101.

  * 🔬 **COPY-ISOLATION HARNESS LANDED 2026-05-31 — the per-eye-split "copy doesn't populate" blocker likely rests on an UNRELIABLE diagnostic; re-test with the reliable one.** (fork `d149a614b`, alvr `08d412a4`.) Re-reading the 2026-05-30 ~21:00 blocker: its entire verdict ("the D3D12 copy transfers nothing") is `src_image_dump=0/4096` — and **this same investigation already flagged `src_image_dump` as an unreliable negative TWICE**: (i) the `e920a094c` entry below — "barriers the image from `UNDEFINED` oldLayout, which can discard contents"; (ii) the 2026-05-30 ~11:30 entry — even with the oldLayout fixed, the dump reads "an image *different* from what the squasher actually samples," and hello_xr (visually correct) ALSO dumped all-zero through it. So the blocker may be a **measurement artifact**, not a real copy failure. The RELIABLE diag is `dump_squash_view_tile` (squasher OUTPUT scratch via `chl_scratch_get_image`, COLOR_ATTACHMENT_OPTIMAL oldLayout) — it cleanly separated hello_xr (`4096/4096`) from AK (`1024/4096` clear-only) on 2026-05-30 ~12:30 — and it was **never applied to the per-eye copy**. Also re-derived the sync model and **disproved a cross-queue-sync worry**: `c->app_queue` IS the app's own render queue (from the D3D12 graphics binding), the copy command list executes on that same serial queue at `release_image`, and `layer_commit` signals the fence on it afterward — so the copy is correctly ordered after the app's render. Sync is not the issue.
    - **Landed (both opt-in, no default behavior change):** (1) `comp_d3d12_client.cpp` — `D3D12_COMPOSITOR_FORCE_COPY` env forces the existing app→comp `compositorNeedsCopy` path on EVERY swapchain at full (same) dims (fixWidth/fixHeight stay false → comp allocated at app dims, `comp_uv_scale`=1.0, full-frame `createCommandListImageCopy`). (2) `comp_alvr.c` — gated the always-on atlas-bypass blit behind `ALVR_ATLAS_BYPASS` (default OFF), so the **real** squasher-sample path runs by default and `dump_squash_view_tile` reflects the squasher, not the blit. Gate A green (clean build, 52s; `openxr_monado.dll` 21:19:07 + `monado-service.exe` 21:19:11 relinked together — matching git-tags; `alvr_server_openxr.dll` unchanged, no bridge-ABI touch).
    - **THE DECISIVE EXPERIMENT (run on .101 + Quest):** deploy the 2 rebuilt binaries (`openxr_monado.dll` + `monado-service.exe`) to `C:\alvr\test-openxr\`, set `D3D12_COMPOSITOR_FORCE_COPY=1` in the hello_xr launch env, run `hello_xr -g D3D12` (per-eye 2144, imports fine directly), and read `squash_view_dump` in the monado log. **Decision:** `non_zero` high (content) ⇒ the D3D12 app→comp copy DOES populate a ≤4096 comp texture ⇒ per-eye-split is viable and the prior block was a measurement artifact ⇒ proceed to wire the 1→2 per-eye split (per the blocker's "rest is mechanical" plan). `non_zero` clear-only (`~1024/4096`, the `(0,0,0,255)` clear) ⇒ the copy mechanism itself is genuinely broken ⇒ fix the copy first (the `createCommandLists` GENERIC_READ vs copy-path COMMON state inconsistency noted below is the first suspect). **Use `squash_view_dump`, NOT `src_image_dump`, to read the result.**
    - **Note vs the prior NEXT-SESSION PLAN:** this supersedes that plan's step-2 ("check `src_image_dump`") with the reliable dump, and defers the D3D12 debug layer (step 1) — Monado can't enable it on the app-created device post-hoc anyway, and the reliable dump decides the copy-works question without it.

  * 🚧 **PER-EYE-SPLIT FIX — STARTED, BLOCKED on the D3D12 compositor-copy not populating 2026-05-30 ~21:00 (all WIP reverted; nothing committed beyond `e920a094c`+`a0c80ba37`).** With the 4096 root cause known, started the fix: split AK's >4096 atlas into per-eye (≤4096) comp textures via `client_d3d12_create_swapchain`'s `compositorNeedsCopy` path + a new `createCommandListImageCopyRegion` (region copy of one atlas half). **Foundational blocker hit: the D3D12 copy does NOT populate the per-eye comp texture** — `src_image_dump` stays `0/4096` even though the comp texture is now correctly `w=2144 bits=0x61` (COLOR|SAMPLED) and imports with zero Vulkan errors. Per-eye COLOR|SAMPLED textures demonstrably import WITH content when an app renders directly into them (hello_xr), so the import is fine — **the copy itself transfers nothing.** Three variants tried, all `src_image_dump=0`: (a) comp bits = SAMPLED-only (existing code's override); (b) comp bits = COLOR|SAMPLED, copy dst state = COMMON; (c) comp bits = COLOR|SAMPLED, copy dst state = RENDER_TARGET (matching the comp's actual creation state). None populated.
    - **Leading suspicion: the existing Monado `compositorNeedsCopy` mechanism may never have been validated with real content** — it only triggers for `info->width < 256 && !isPowerOfTwo` (tiny images, essentially never in practice), so the app→comp `CopyTextureRegion`-on-release path (`client_d3d12_swapchain_release_image_copy`) could be subtly broken (resource-state coordination between the app render queue, the copy, and the cross-API sampled state; note `createCommandLists` uses `compositorResourceState = GENERIC_READ` while the copy path hardcodes `COMMON` — an inconsistency worth chasing).
    - **NEXT-SESSION PLAN (needs the D3D12 DEBUG LAYER — without it these state errors are silent):** (1) enable the D3D12 debug layer in `client_d3d12_create_swapchain` (or via the app) to catch the copy's invalid barriers/copy directly. (2) ISOLATE whether the copy MECHANISM works at all: force `compositorNeedsCopy` + a FULL `createCommandListImageCopy` on a SMALL app (hello_xr `-g D3D12`, 2144, which imports fine directly) and check `src_image_dump` — if the full-copy comp is black, the copy mechanism is broken (fix that first); if it has content, the bug is region-copy-specific. (3) confirm `client_d3d12_swapchain_release_image_copy` is actually invoked for AK (1-line counter) and that `data->images[i]` (copy SRC) aliases the app-rendered memory. Once the copy populates, the rest is mechanical: add a second (right-eye) comp swapchain + rewrite `client_d3d12_compositor_layer_projection`/`_depth` to point view 0→left native, view 1→right native (both full norm_rect) — `client_d3d12_compositor_layer_projection` already intercepts + rewrites per-view sub-images, so NO comp_alvr changes are needed.
    - **DURABLE: per-eye-split's hard part is the D3D12 app→comp copy, not the Vulkan import. The import is solved (≤4096 works). Resume by making the copy populate, with the debug layer on.**

  * 🎯 **ROOT CAUSE NAILED 2026-05-30 ~20:30 — D3D12→Vulkan shared-image import on this RTX 3090 produces BLACK for textures WIDER THAN 4096. Boundary bracketed to `(4096, 4288]`.** Width binary-search via forcing the advertised per-eye recommended width (override `rec_w` in `comp_alvr.c::compositor_init_sys_info`, reverted) and watching `hello_xr -g D3D12`'s `src_image_dump`:
    - per-eye **2144** → `non_zero=4096/4096` (content) ✅
    - per-eye **4096** → `alvr_from_natives w=4096`, `src_image_dump non_zero=4096/4096` (content) ✅
    - AK atlas **4288** → `non_zero=0/4096` (black) ❌
    So **4096 works, 4288 black** — a razor-thin boundary at 4096 = 2¹², the canonical GPU max-2D-texture / tiling-pitch limit. **The bug is a hard 4096 max shared-texture WIDTH for D3D12→Vulkan import on NVIDIA.** AK's UE5 stereo atlas (4288 = 2×2144, a single texture) exceeds it by 192px; everything ≤4096 imports fine. This fully explains the whole saga (not sync, not handle-type, not memory-visibility, not the squasher). NB the ALVR authors already set `MAX_VIEW_WIDTH = 4096` (comp_alvr.c:56) — but that's the *per-eye* cap; UE5's *atlas* is a single 2-eye texture that blows past it.
    - **THE FIX (clear + de-risked): split any shared D3D12 swapchain wider than 4096 into ≤4096-wide pieces before import.** For AK's 4288 atlas → two per-eye 2144 textures (proven to import). This is the per-eye-split, now fully justified: `client_d3d12_create_swapchain` allocates two per-eye comp textures, `CopyTextureRegion` each atlas half into them per frame, import the two handles, and present them to the squasher as per-eye (sc_array[0]/[1] = the two comp textures, norm_rect {0,0,1,1} each). The earlier per-eye-COPY probe was on the right track — it just needs (a) a WORKING copy (the probe's copy didn't populate — debug `client_d3d12_swapchain_release_image_copy` resource states / region) and (b) the two-texture presentation wired into the layer (the probe only made one). Alternative simpler framing: a generic "if `info->width > 4096`, allocate N=ceil(width/4096) horizontal tiles" splitter in comp_d3d12_client, but per-eye (N=2) is the natural fit for stereo atlases.
    - **DURABLE: the magic number is 4096 — any single D3D12 shared texture wider than that imports black via D3D12_RESOURCE_BIT on this NVIDIA driver. Keep all shared textures ≤4096 wide.** **DURABLE: the width-probe harness is `rec_w` override in `comp_alvr.c::compositor_init_sys_info` + `hello_xr -g D3D12` (task `alvr_diag_helloxr_d3d12`); MAX_VIEW_WIDTH must be ≥ the test width.**

  * ✅ **CORRECTION 2026-05-30 ~20:15 — bug is WIDTH/ATLAS-specific, NOT D3D12-path-fundamental. `hello_xr -g D3D12` (per-eye, direct render) imports with FULL content.** The "D3D12-path-fundamental" conclusion in the entry below was WRONG. Decisive test: ran the Khronos `hello_xr -g D3D12` (per-eye D3D12 swapchains, rendered directly, no atlas/copy/UE5) against ALVR-Monado. Result: `alvr_from_natives w=2144 handle_source=2`, `src_image_dump non_zero=4096/4096 first_16=779797ff` (real content), `squash_view_dump non_zero=4096/4096` — **per-eye D3D12 import works end-to-end through the same shared Monado import code that imports AK's atlas black.** So:
    - The `D3D12_RESOURCE_BIT` fix (`e920a094c`/`a0c80ba37`) is GENUINELY CORRECT and sufficient for per-eye D3D12.
    - **`data->images` (the app textures hello_xr/AK render into) are created by the SAME `allocateSharedImages` (comp_d3d12_client.cpp:502) for both apps.** hello_xr's are per-eye (2144) → content; AK's are wide atlas (4288) → black. **Same creation, same import — the ONLY variable is width.** There is a width threshold (somewhere between 2144 and 4288, plausibly 4096) above which NVIDIA's D3D12↔Vulkan shared-image layout diverges and the import samples black.
    - **The per-eye-split COPY probe's black was a RED HERRING — a copy-not-populating bug, NOT an import failure** (per-eye D3D12 import demonstrably works). Per-eye-split is therefore VIABLE once the copy is fixed.
    - **NEW recommended directions (in order):** (1) **Find the width boundary** — binary-search the failing width (e.g. force AK/hello_xr swapchains to 2560/3072/4096/4288 and watch `src_image_dump`); if it's a clean 4096 limit it may be a small fixable bug in Monado's VkImage creation (extent/row-pitch) that fixes ALL wide swapchains. (2) **Per-eye-split done right** — copy AK's atlas halves into two ≤2144 comp textures (which import fine) and present per-eye; the earlier probe just needs a working copy. **DURABLE: `hello_xr -g D3D12` (via `run_hello_xr_d3d12.cmd` / task `alvr_diag_helloxr_d3d12` on .101) is the minimal D3D12 reproducer — per-eye works, so any "D3D12 is broken" theory is wrong; the variable is WIDTH.**
    - The strikethrough entry below is preserved for the reasoning trail but its headline conclusion is SUPERSEDED by this correction.

  * ~~🔬 **AK black-screen: FOUR hypotheses eliminated 2026-05-30 ~19:40 — bug isolated to D3D12→Vulkan import being FUNDAMENTALLY BROKEN**~~ (SUPERSEDED — see correction above; the bug is width-specific, per-eye D3D12 works). This session implemented + tested the proper D3D12_RESOURCE_BIT fix (LANDED, see entry below) then ran it through a systematic elimination. **Nothing new committed beyond `e920a094c`+`a0c80ba37`** — the experiments below were probes, all reverted. Definitive findings:
    1. **Handle type — FIXED but insufficient.** `handle_source=2` (D3D12_RESOURCE_BIT) now reaches the server and imports with **zero Vulkan errors**. But AK's atlas still samples black. So the missing D3D12_RESOURCE_BIT path was a real bug, just not the (only) cause of black.
    2. **Sync race — DISPROVEN.** `alvr_wait_sem_diag` in `comp_multi_compositor.c::wait_semaphore` shows `peek=0 peek_done_total=0 dur_us≈3500 iters=1` across 18 000+ frames. The wait_thread genuinely **blocks ~3.5 ms each frame** on AK's GPU-completion semaphore and NEVER returns early. comp_alvr does NOT sample before AK's GPU finishes. The 3.5 ms also proves **AK renders real content** (a black clear is <0.1 ms). The earlier "sync race" framing (entry below) was WRONG — the data refutes it.
    3. **GPU memory visibility — DISPROVEN.** Hypothesis: the multi_compositor does a HOST semaphore wait (no GPU-side cache flush), so the squasher samples stale memory. Added a per-frame `VK_QUEUE_FAMILY_FOREIGN_EXT` ownership-acquire barrier on AK's source images before the squasher (`acquire_layer_sources_from_external` in comp_alvr.c, reverted). It ran cleanly (`alvr_ext_acquire result=0`) — source STILL `non_zero=0/4096`. Memory visibility is not the issue.
    4. **Width / per-eye-split — DISPROVEN.** Hypothesis: NVIDIA's D3D12↔Vulkan layout mismatch is width-specific (per-eye 2144-wide works like hello_xr; 4288 atlas doesn't). Probe: allocate a per-eye-WIDTH (2144) D3D12 comp texture, D3D12-copy the atlas's left half into it, import that (`createCommandListImageCopyRegion` + isWideStereo branch in comp_d3d12_client.cpp, both reverted). Result: `alvr_from_natives w=2144` confirms the per-eye texture is imported, but `src_image_dump non_zero=0/4096` — **per-eye D3D12 ALSO imports black.** Not width.
    - **CONCLUSION: the common factor in EVERY black case is D3D12; the only WORKING case (hello_xr) is D3D11/KMT.** AK's own wide atlas (rendered directly, NO copy) imports black; a wide Monado comp D3D12 texture (full CopyResource) imports black; a per-eye Monado comp D3D12 texture (region copy) imports black. hello_xr per-eye D3D11/KMT works. So the divide is **D3D11/KMT (works) vs D3D12_RESOURCE_BIT (black)**, NOT atlas-vs-per-eye and NOT width. The D3D12→Vulkan shared-image import path itself is producing black on this RTX 3090 / Monado combo, despite correct `D3D12_HEAP_FLAG_SHARED` + `CreateSharedHandle(GENERIC_ALL)` + committed resource + `D3D12_RESOURCE_BIT` import.
    5. **`D3D12_TEXTURE_LAYOUT_ROW_MAJOR` — TESTED 2026-05-30 ~20:00, DEAD END.** Flipped Monado's `d3d_d3d12_allocator.cpp` shared-texture layout from `UNKNOWN` (driver-swizzled) to `ROW_MAJOR` (linear) and re-enabled the compositor-copy for AK's COLOR atlas (gated `XRT_SWAPCHAIN_USAGE_COLOR && !DEPTH_STENCIL` so the depth atlas keeps UNKNOWN). Result: AK's `xrCreateSwapchain` returned **`XR_ERROR_RUNTIME_FAILURE`** and AK **crashed** (`Assertion failed: IsValid()` in `FOpenXRSwapchain::CreateSwapchain` → `RHISubmissionThread` crash; see `ak.out`). The `ROW_MAJOR` 2D shared texture is rejected at `CreateCommittedResource`/import on this RTX 3090 (NVIDIA does not support ROW_MAJOR 2D textures for this usage), so the linear-layout escape hatch is unavailable. Reverted; `.101` restored to the clean committed build. **DURABLE: ROW_MAJOR 2D shared textures are not creatable here — do not retry this angle.**
    - **REMAINING CANDIDATE DIRECTIONS (unverified, harder):**
      1. **Route D3D12 content through the working D3D11/KMT path (D3D11On12).** Wrap AK's D3D12 device with a `D3D11On12` device, get D3D11 textures backing the atlas (or copy into D3D11 comp textures), share via the DXGI/KMT path that hello_xr proves works. Heavier, but directly leverages the one path known to work on NVIDIA.
      3. **Upstream Monado D3D12-interop investigation.** This may be a known Monado/NVIDIA limitation; check Monado's tracker / test D3D12 swapchains against stock comp_main on the same GPU to confirm it's not ALVR-specific.
    - **DURABLE: `alvr_wait_sem_diag` (peek + dur_us) is the definitive "is the GPU-sync wait effective" probe — peek=0 + dur_us≈frame-time = the wait works; peek=1 + dur~0 = no-op race.** **DURABLE: AK's OWN directly-rendered atlas imports black with no copy involved — so any black-screen theory that depends on the copy/squasher path is wrong; the failure is in the raw D3D12→Vulkan import.** **DURABLE: rebuilding only one of {openxr_monado.dll, monado-service.exe} and redeploying gives a git-tag mismatch (`pipe_print_get_last_error ReadFile: 109`, AK disconnects pre-render); ALWAYS force BOTH to relink in one `build-openxr-runtime` invocation (touch a client source AND comp_alvr.c) and deploy both.**

  * ✅ **D3D12_RESOURCE_BIT API change LANDED + RETEST RESULT 2026-05-30 ~19:00 (fork `e920a094c` + `a0c80ba37`, alvr submodule bump + this doc). Import half FIXED & PROVEN; ~~headset still black due to a SEPARATE sync race~~ — SUPERSEDED: the "sync race" was DISPROVEN by the entry above; bug is D3D12-path-fundamental.** The proper upstream fix the prior entry called for.
    - **RETEST on .101 + Quest + AK.exe**: after deploying the rebuilt 3 binaries, the server logs `alvr_from_natives ... handle_source=2` (D3D12) for AK's 4288×2240 atlas swapchains, the `D3D12_RESOURCE_BIT` import **succeeds with zero Vulkan errors** (NVIDIA accepts it), and the encoder produced **real-content bursts of 589–1110 bytes/packet** — impossible before (every prior AK run was pinned at the ~27–38 byte black floor). So the handle-type bug is genuinely fixed: caught frames now contain valid content instead of layout-garbage-zero.
    - **BUT the headset is still (intermittently) black**, and so is AK's PC-monitor mirror. The bitstream pattern is brief real bursts right after connect, then sustained ~27 bytes/packet (black), then a Quest disconnect ~60s in. This is the **separate `multi_compositor` GPU-completion sync race** from the prior entries (comp_alvr samples AK's atlas BEFORE AK's GPU work lands, so it usually grabs an empty image, occasionally catching a completed frame = the bursts). The D3D12 fix is **necessary but not sufficient** — it fixes the handle-type half; the sync half (the `xrt_comp_import_fence` / wait_thread path for AK's NT-shared D3D12 fence) is still open. **NEXT BUG TO FIX.**
    - **`src_image_dump non_zero=0` is NOT a reliable negative** — that probe barriers the image from `UNDEFINED` oldLayout, which can discard contents (the code even comments on it). Don't trust it as a black/real signal; use the encoder bitstream instantaneous rate (total_bytes delta / packets delta) instead — `~27 = black`, `500+ = real`.
    - **The IPC transport had to change too (`a0c80ba37`)**: the first cut (`e920a094c`) smuggled the source in the handle's low 2 bits, which only works for DXGI (passed by value); NT/D3D12 handles are `DuplicateHandle`'d into the server and come out with clean low bits (kernel ignores them), so the source was lost (server saw `handle_source=0`). Fixed by carrying it in `ipc_arg_swapchain_from_native::handle_sources[]` (marshalled by value like `sizes[]`); bit 0 kept ONLY as the DXGI "don't duplicate" transport directive. **DURABLE: you cannot smuggle bits in a Win32 NT handle across Monado IPC — it gets re-duplicated. Use the args struct.**
    - **DURABLE: the chronic Quest ~60s disconnect (`lifecycle_check_thread: client !Streaming` / os error 10053/10054) truncates every AK test before UE5 stabilizes — measure fast or fix the disconnect first.**
    Original implementation notes below. Replaced the two-state `bool is_dxgi_handle` on `xrt_image_native` with a three-state `enum xrt_image_native_handle_source { XRT_IMAGE_NATIVE_HANDLE_SOURCE_OPAQUE=0, _DXGI=1, _D3D12=2 }`. The boolean could only say "DXGI or not", forcing D3D12-shared resources down the OPAQUE_WIN32 import path that samples as all-zero for wide stereo atlases on NVIDIA. Eight `src/` files + four `tests/` fixtures changed together (the struct field change forces it):
    - **`xrt_compositor.h`**: enum definition + `handle_source` field (replaces `is_dxgi_handle`).
    - **`vk_compositor_flags.c::vk_csci_get_image_external_handle_type`**: 3-way switch → `OPAQUE_WIN32_BIT` / `D3D11_TEXTURE_KMT_BIT` / **`D3D12_RESOURCE_BIT`**.
    - **`vk_image_allocator.c`**: ref/unref of NT handles keyed on `source != DXGI` (D3D12 refs like OPAQUE — both are reference-counted NT handles; only the import handle TYPE differs). Diag prints `handle_source` not `is_dxgi`.
    - **`d3d_d3d11_allocator.cpp`** → sets `DXGI`; **`d3d_d3d12_allocator.cpp`** → sets `D3D12`; **`comp_d3d_common.hpp`**: `importFromHandleDuplicates` (the sole D3D12-client import path, AK's path) sets `D3D12`, `importFromDxgiHandles` (D3D11) sets `DXGI`.
    - **IPC transport** (`ipc_client_compositor.c` / `ipc_server_handler.c`): widened the existing low-bit-in-handle hack from 1 bit to 2 bits (Win32 NT handles are ≥4-byte aligned, low 2 bits free) to carry the 3-state source across the client↔server boundary. OPAQUE encodes to 0 → handle bit-identical to the old scheme, so existing OPAQUE/DXGI traffic is unchanged.
    - **Validation that the fix is on the right track**: Monado's own `vk_create_image_from_native` (`vk_helpers.c:1233-1235`) ALREADY groups `D3D12_RESOURCE_BIT` with the D3D11 texture types for its memory-size-check skip — the low-level import code anticipated this handle type; only the selector in `vk_csci_get_image_external_handle_type` never returned it. The support gate at `vk_helpers.c:1115` queries `vkGetPhysicalDeviceImageFormatProperties2` with the handle type, so if NVIDIA doesn't report D3D12_RESOURCE_BIT importable it fails cleanly with "External memory handle is not importable" rather than another silent black.
    - **Build**: `cargo xtask build-openxr-runtime --enable-alvr-driver` clean on the AMD host (monado-service.exe + all 4 D3D test exes link). The `alvr_server_openxr` cdylib is untouched (the struct is internal to Monado; the bridge ABI doesn't reference it).
    - **VERIFICATION CEILING**: behaviour can only be confirmed on .101 (RTX 3090) + Quest + AK.exe — the AMD host has no NVIDIA D3D12↔Vulkan layout divergence to reproduce the bug, and no headset. **Expected on retest**: `dump_squash_view_tile` shows `non_zero > 0` (real content, not `0/4096`), encoder bitstream `avg_bytes_per_packet` climbs from ~38 (black) to 50K+ (real), and AK renders in the headset. If instead `xrCreateSwapchain` fails with "not importable", NVIDIA's Vulkan doesn't support D3D12_RESOURCE_BIT import for that format and the per-eye-split copy workaround (prior entry's "alternative not tried") becomes the fallback.

  * ✅ **compositor-copy workaround attempt — DOESN'T HELP 2026-05-30 ~16:30** (fork commits `bca2121c0`+`a7fd11482`, alvr `2ae3b871`+`68f5dce4`). Per user direction "try the compositor copy workaround but if it will be ok we will change api". Extended `compositorNeedsCopy` heuristic in `comp_d3d12_client.cpp::client_d3d12_create_swapchain` to also fire for wide stereo atlases: `isWideStereo = (2 * info->width >= 3 * info->height) && info->width > 2048`. File-write diag (because UE5 intercepts fprintf-to-stderr from openxr_monado.dll — durable trap) confirmed end-to-end flow: AK reaches `client_d3d12_create_swapchain` for color (format=91, R8G8B8A8_TYPELESS) and depth (format=20, R32_TYPELESS), both 4288×2240; `isWideStereo=1`/`compositorNeedsCopy=1` triggers; the workaround allocates fresh comp_images, builds copy command lists, imports comp_handles via `ipc_compositor_swapchain_import`/`vk_ic_from_natives` (server side `alvr_from_natives` log confirms new handle values, still `is_dxgi=0`). **But `dump_squash_view_tile` STILL shows `non_zero=0/4096 first_16=00000000` — pure zeros.** So the bug is **invariant under app-vs-comp allocation**: any D3D12-allocated wide-stereo NT handle, regardless of when/where it was allocated in the D3D12 device's lifetime, imports via OPAQUE_WIN32_BIT as empty memory. Fresh allocation doesn't produce a different memory layout. Reverted. **User criterion ("if ok"): NOT OK → need the API change.** Two additional durable findings from this round: (1) **UE5 intercepts `fprintf(stderr,...)` from openxr_monado.dll** — only file-writes to a known path reliably surface log messages from the client side. (2) **AK genuinely uses D3D12 binding** — `alvr_d3d12_create_swapchain` fires correctly. The IPC client's `ipc_compositor_swapchain_create` is NOT called for AK; only `ipc_compositor_swapchain_import` (the D3D12 wrapper allocates D3D12 textures locally then registers via import_swapchain). **Alternative workaround not tried** (out of this iteration's scope): extend `compositorNeedsCopy` to allocate per-eye comp_images (1-to-2 mapping with `vkCmdCopyImage` per-eye-region from atlas halves) — since per-eye dims via OPAQUE_WIN32 are known to work (hello_xr 2144×2240 visually correct), this might bypass the wide-stereo layout issue while staying within the existing handle-type framework. ~3-5 hours of work. **The proper upstream fix remains: add `VK_EXTERNAL_MEMORY_HANDLE_TYPE_D3D12_RESOURCE_BIT` path to `vk_csci_get_image_external_handle_type`** (requires enum-typed handle source on `xrt_image_native` + IPC transport upgrade + new code path).

  * ✅ **is_dxgi=true experiment outcome — DEFINITIVE: bug is missing `VK_EXTERNAL_MEMORY_HANDLE_TYPE_D3D12_RESOURCE_BIT` path in Monado 2026-05-30 ~15:20** (fork commits `024069543`+`54e24f9cc`+`a3a54e1cb`, alvr `d49c542a`+`afe000db`+`6f2dd948`). Two diagnostic discoveries this round, both important: (1) **AK is D3D12, not D3D11** (`LogD3D12RHI: Creating D3D12 RHI with Max Feature Level SM6` / `rhiname="D3D12"` in all four `ak*.out` snapshots) — so it goes through `client_d3d12_create_swapchain` → `importFromHandleDuplicates`, NOT through `client_d3d11_create_swapchain` → `importFromDxgiHandles`. (2) **`importFromHandleDuplicates` was leaving `is_dxgi_handle` defaulted to false** (`comp_d3d_common.hpp:60-91` builds `xrt_image_native xin{}` and never sets `is_dxgi_handle`), while `importFromDxgiHandles` (D3D11 path, same file line 122) explicitly sets it to true. So D3D11 clients route through `VK_EXTERNAL_MEMORY_HANDLE_TYPE_D3D11_TEXTURE_KMT_BIT`; D3D12 clients silently fall through to `OPAQUE_WIN32_BIT`. Test: flipped `is_dxgi_handle = true` in `importFromHandleDuplicates`, redeployed, retest with AK. **Result: Vulkan import failed explicitly** with `VK_ERROR_OUT_OF_DEVICE_MEMORY` in `vk_alloc_and_bind_image_memory` (called by `vk_create_image_from_native`); AK's `xrCreateSwapchain` returned `XR_ERROR_RUNTIME_FAILURE`. So D3D12-shared NT handles are NOT compatible with the `D3D11_TEXTURE_KMT_BIT` import type — Vulkan validation correctly rejects the mismatch. Reverted. **Bug now precisely characterized**: Monado's `vk_csci_get_image_external_handle_type` (`vk_compositor_flags.c:214-226`) has only 2 Windows paths (`D3D11_TEXTURE_KMT_BIT` vs `OPAQUE_WIN32_BIT`); neither is correct for D3D12-shared resources. The correct Vulkan handle type is `VK_EXTERNAL_MEMORY_HANDLE_TYPE_D3D12_RESOURCE_BIT` (or `D3D12_HEAP_BIT`), which Monado doesn't use anywhere (only mentioned in a comparison check at `vk_helpers.c:1235`). For per-eye D3D12 chains the `OPAQUE_WIN32` fallback "works" by accident (small dims happen to have matching memory layout between D3D12 and Vulkan on NVIDIA); for wide stereo atlases the layouts diverge and Vulkan reads zeros. **NB: Monado authors knew about this exact bug class** — see `comp_d3d12_client.cpp:567-580` "There is a bug in nvidia systems where D3D12 and Vulkan disagree on the memory layout of smaller images" with a workaround that only triggers for `width < 256 && !isPowerOfTwo(width)`. AK's 4288-wide atlas falls outside the existing workaround scope. **Proper upstream fix needed**: (a) add `enum xrt_image_native_handle_source { OPAQUE, D3D11, D3D12 }` to `xrt_image_native` (xrt_compositor.h:2190); (b) D3D12 client sets it to D3D12 in `importFromHandleDuplicates`; (c) IPC layer transports the enum (current `is_dxgi_handle` bool + low-bit-in-handle-pointer hack at `ipc_client_compositor.c:340-342` / `ipc_server_handler.c:1786-1789` needs upgrade); (d) `vk_csci_get_image_external_handle_type` returns `D3D12_RESOURCE_BIT` for D3D12. Alternative workaround in `comp_d3d12_client.cpp`: extend the existing `compositorNeedsCopy` workaround condition to also trigger for wide stereo atlases (any `width > 2048` or similar threshold) — would create per-eye-sized compositor images and `vkCmdCopyResource` from the app atlas. Snapshot: `.101:C:\alvr\test-openxr\monado.20260530_152138.is_dxgi_true.log`.

  * ✅ **vk_ic_from_natives diag confirms AK uses 4288×2240 atlas + identifies NT-handle import path 2026-05-30 ~14:30** (fork `79bf32a51`, alvr `aeb54211`). Initial vk_ic_allocate instrumentation fired only on Monado-side allocations (`c->scratch` + `c->ffr.scratch` at 2144×2240 and 1280×1184) — NOT AK's swapchains. D3D11 OpenXR clients (UE5/AK) go through a different path: `client_d3d11_create_swapchain` (in AK's process, via openxr_monado.dll) → `allocateSharedImages` (creates D3D11 textures on Monado's `comp_device`) → DXGI shared NT handles → `importFromDxgiHandles` → IPC to monado-service → **`vk_ic_from_natives`**. Added diag there. **Smoke confirms AK creates exactly two atlas swapchains**: `w=4288 h=2240 format=50 image_count=3 is_dxgi=0` (color atlas, ~39.5 MB/image) + `w=4288 h=2240 format=130 image_count=3 is_dxgi=0` (depth atlas, ~79 MB/image). pack_projection_layer's atlas interpretation (atlas=1, sc[0]==sc[1], view 0 = left half {0,0,2144,2240}, view 1 = right half {2144,0,2144,2240}) is correct — AK genuinely uses a single 4288-wide image divided per view. **New key suspect — `is_dxgi=0` (NT handle) path**: Per `xrt_compositor.h:2181-2190` doc comment, `is_dxgi=false` means "reference counted NT Handle which has awkward limitations, such as usually no depth images allowed", vs `is_dxgi=true` = "weird global handle, not reference counted, widely compatible with various images. Ostensibly deprecated, but **works the best on Windows**". AK's COLOR atlas (not just depth) imports as NT handle. The bug may be specifically about cross-process NT-handle shared D3D11→Vulkan import for **wide stereo atlases**. Per-eye chains via NT handle work (hello_xr is per-eye and visually correct via this same pipeline). Wide atlases via NT handle return empty when sampled or blitted. Possible fixes: (1) force `is_dxgi=true` for color atlases in `comp_d3d11_client.cpp::client_d3d11_create_swapchain` → uses DXGI shared handles which the comment says work best on Windows; (2) add a sync barrier or queue ownership transfer the NT-handle path doesn't enforce; (3) detect atlas width > some threshold and split into per-eye internally. Snapshot: `.101:C:\alvr\test-openxr\monado.20260530_145926.from_natives.log`.

  * ✅ **AK per-eye override attempt — NOT FEASIBLE 2026-05-30 ~14:00**: AK is a cooked shipping UE5 build with no exposed Config dirs (no `DefaultEngine.ini`, no `DefaultGame.ini`, no `Saved\Config\Windows`). Tried `-ExecCmds="vr.OpenXR.UseTextureArray 1; vr.OpenXRRenderTargetType TextureArray"` on the AK.exe command line — no relevant log lines in `ak.pereye.out`, and `pack_projection_layer` continues to report `atlas=1 sc[0]==sc[1] rect={x,0,2144,2240}` identically to all prior runs. AK is locked into atlas mode by its build. We can't force AK to per-eye from outside its binary. To test the per-eye-works hypothesis from an opposite angle would require: (1) a different UE5 OpenXR game that uses texture-array / per-eye chains (not available), (2) modifying AK's binary (impractical), or (3) building a minimal D3D11 atlas reproducer from scratch (separate effort). The atlas bug stands as found in the prior blit-bypass result. Snapshot: `.101:C:\alvr\test-openxr\monado.20260530_140437.ak_pereye_attempt.log`. **Durable learning**: shipped UE5 binaries don't accept cvar overrides via `-ExecCmds` for OpenXR rendering layout — those cvars are read at engine init before plugin loading completes, and a cooked build doesn't ship the OpenXR plugin's INI-config-merge path that would pick them up.

  * ✅ **comp_main vs comp_alvr atlas-handling comparison 2026-05-30 ~13:30**: NO architectural distinction found. Both compositors inherit `base_create_swapchain` → `comp_swapchain_create` from `comp_base`; both use `chl_frame_state_gfx_*` from `comp_high_level_render`; both end up in `comp_render_gfx_dispatch` → `do_projection_layer` → `render_gfx_layer_projection`; both enable the same `VK_KHR_EXTERNAL_MEMORY_WIN32` + `DEDICATED_ALLOCATION` + `GET_MEMORY_REQUIREMENTS_2` + `EXTERNAL_SEMAPHORE_WIN32` + `EXTERNAL_FENCE_WIN32` extensions (verified line-by-line: `comp_alvr.c:124-156` vs `comp_compositor.c:516-552`); both set `client_vk_deviceUUID` + `client_d3d_deviceLUID` for cross-process GPU matching; both allocate Vulkan images via `vk_ic_allocate` and export NT handles via `vk_ic_get_handles`. Only call-site difference: comp_main passes the real display swapchain as distortion target; comp_alvr passes a 1x1 dummy — but the distortion pass writes the final composited output (discarded by comp_alvr), it doesn't touch the squasher's source-image sampling path. So **the bug is NOT something comp_alvr added relative to comp_main**. It's in shared code (comp_swapchain_create / vk_image_allocator for wide-stereo D3D11-sharable images) OR in cross-process D3D11 import on AK's side (outside Monado). The premise "comp_main works for native-Monado UE5 users running atlas chains" is unconfirmed — possibly atlas-mode UE5 was never broadly tested with comp_main either, and Monado has a latent atlas-import bug that's only now surfacing because we ran an actual UE5 game (AK) through it. Next investigation directions all live OUTSIDE comp_alvr: (i) instrument `vk_ic_allocate` for AK's atlas swapchain create to check the actual `VkImageCreateInfo` parameters and external-memory image-format-properties results; (ii) check if forcing AK to per-eye chains (UE5 ini setting / env var / mod) works; (iii) check upstream Monado bug tracker for known atlas/wide-stereo D3D11 import issues; (iv) test against a different Monado version (newer or older) to bisect when atlas import last worked.

  * ✅ **vkCmdBlitImage bypass DEFINITIVELY isolates the bug to D3D11→Vulkan IPC import 2026-05-30 ~13:10** (fork `d8a574fd7`, alvr `4e56beed`). New `alvr_blit_atlas_bypass_view` in `comp_alvr.c::compose_via_squasher` runs right after the squasher's `vkQueueWaitIdle` (post-squash, pre-FFR). For each view it transitions the IPC-imported atlas image `SHADER_READ_ONLY_OPTIMAL → TRANSFER_SRC_OPTIMAL`, transitions scratch `COLOR_ATTACHMENT_OPTIMAL → TRANSFER_DST_OPTIMAL`, then `vkCmdBlitImage` from the atlas's per-view sub_rect (view 0: {0,0,2144,2240}, view 1: {2144,0,2144,2240}) to the full scratch, then restores both layouts. Pure transfer — no samplers, no descriptors, no UV math, no fragment shader. **Smoke result on .101+Quest+AK**: the bypass fires per-view per-frame (logs confirm), and `dump_squash_view_tile` AFTER the bypass shows `non_zero=0/4096 first_16=00000000` — **pure zeros, including alpha**. (Compare to the prior squash-only dump which showed `non_zero=1024/4096 first_16=000000ff` — that was the render-pass clear-color (R=0,G=0,B=0,A=255). The blit really did overwrite, and what it overwrote with was all-zero memory.) **Therefore: AK's IPC-imported atlas image is genuinely empty when Monado's Vulkan reads it.** Not a sampler / view / UV / shader / draw-call bug — none of those apply to `vkCmdBlitImage`. The D3D11→Vulkan IPC import itself doesn't expose AK's D3D11 atlas writes to Monado's Vulkan view. **Most likely root causes** (now reachable for targeted fixes): (1) Monado's IPC client D3D11 swapchain import allocates fresh Vulkan-backed memory that never gets bound to AK's actual D3D11 atlas texture (the "shared" handle is structurally broken for atlas chains); (2) DXGI keyed-mutex / shared NT-handle stride/format mismatch for wide atlas textures (4288 wide may exceed some limit specific to the import path); (3) the `xrt_compositor_semaphore_wait` signal we proved correct earlier only ensures AK's GPU work completed in AK's D3D11 process, NOT that the writes are visible across the NT-handle share to Monado's process (cross-process D3D11→Vulkan import ordering). hello_xr per-eye chains work through identical Monado code → the bug is specifically about how Monado handles atlas-shaped D3D11 imports. AK works through SteamVR (which has its own D3D11→OpenVR path) → it's not AK or the Quest. **This is the deepest layer reachable from comp_alvr** — the next fix needs to live in Monado's `openxr/src/xrt/compositor/client/` (D3D11 IPC swapchain creation) or in the `xrt_swapchain_native` D3D11 import path. Snapshot: `.101:C:\alvr\test-openxr\monado.20260530_130836.blit_bypass.log`. Next experiments (all under `comp/client` rather than `comp_alvr`): (a) inspect AK's create_swapchain IPC call to see actual atlas-image creation flags vs hello_xr's per-eye chains; (b) test if forcing AK to use per-eye chains (UE5 config or env var) bypasses the bug; (c) compare comp_main's atlas handling (which presumably works for users with native Monado HMDs running UE5 content) to comp_alvr's path — `comp_alvr` skipped a bunch of IPC-import setup the main compositor does.

  * ✅ **do_projection_layer + render_gfx_layer_projection both fire correctly for AK 2026-05-30 ~13:00** (fork `304f8c57a`, alvr `e7aa76c1`). Added `alvr_do_proj_layer` diag in `comp_render_gfx.c::do_projection_layer` (after `set_post_transform_rect`) logging view_index, sc_array_index, array_index, image*, image->array_size, src_image_view*, post_transform=(x,y,w,h), flags. Added `alvr_proj_draw` diag in `render_gfx.c::render_gfx_layer_projection` (right before `dispatch_no_vbo`) logging premult, descriptor_set*, pipeline*. **Smoke result on .101+Quest+AK (12 sampled calls each)**: do_projection_layer fires for BOTH views per pack (call 1 view=0, call 2 view=1, etc.), `view_handle=00000176026A06D0` etc — **NON-NULL**, `image->array_size=1`, `post_xform=(x=0.0000 y=0.0000 w=0.5000 h=1.0000)` for view 0 and `(x=0.5000 y=0.0000 w=0.5000 h=1.0000)` for view 1 — **EXACTLY correct atlas split math**. render_gfx_layer_projection fires per do_projection_layer with `desc=000001760251CFD0` (left) and `desc=000001760251D348` (right) alternating, `pipeline=0000017602608B90` — **the draw IS issued with valid descriptor + valid pipeline**. So **every host-side compositor path is healthy for AK**: layer enumeration, image-view resolution, UV transform math, descriptor allocation, draw issuance. Yet the scratch image (read via `dump_squash_view_tile`) still ends up `non_zero=1024/4096 first_16=000000ff…` (clear-color only). Therefore: **the bug is in what the fragment shader actually reads from AK's IPC-imported atlas image** at the moment of sampling. The image_view is correctly built over the full 4288×2240 atlas (`comp_swapchain.c:343-357` creates a 2D view covering the full array layer); the descriptor binds it; the sampler is `clamp_to_border_black`; the post_transform tells the vertex shader to map view 0 to `uv ∈ [0, 0.5]×[0, 1]` and view 1 to `uv ∈ [0.5, 1]×[0, 1]` — both inside the [0,1] sampling range of the full atlas. So if sampling produces (0,0,0,0) for those UVs, **AK's IPC-imported atlas image is empty at sample time** despite `xrt_compositor_semaphore_wait` having returned XRT_SUCCESS (proven correct by today's wait_thread peek diag: 0/6900 waits were no-ops, ~3ms is real GPU completion). Either (1) the semaphore signal happens BEFORE D3D11's writes to the atlas are visible to Vulkan's view (D3D11→Vulkan interop ordering bug specific to shared stereo atlases), or (2) the Vulkan image-view built over the IPC-imported D3D11 atlas doesn't expose AK's writes for structural reasons (KMT vs NT handle, wide-texture limits, ImageType subtleties). **Decisive next experiment**: replace `render_gfx_layer_projection`'s `dispatch_no_vbo` with an explicit `vkCmdBlitImage` (per-view region from the atlas to the scratch image, post-wait). If scratch then shows AK's content → squasher's sampler path is the bug; if still empty → IPC-imported atlas IS empty when sampled, bug is in D3D11→Vulkan interop / sync model. Snapshot on .101: `monado.20260530_125956.draws.log`. Alvr-side bytes/packet 78-1112 finding from earlier sessions = pure-black-encoded-with-IDR-overhead (not real content). The Cordon Rule restated: bytes/packet alone can't distinguish real-but-low-motion from pure-black-with-IDRs; squasher-output dump is the load-bearing diag.

  * ✅ **Squasher per-view scratch dump CONFIRMS atlas bug 2026-05-30 ~12:30** (fork `2fea933f2`, alvr `e800661b`). New `dump_squash_view_tile` in `comp_alvr.c::compose_via_squasher` copies a 32x32 centre tile of each view's scratch image right after `vkQueueWaitIdle` and before `run_ffr` (transitions COLOR_ATTACHMENT_OPTIMAL → TRANSFER_SRC_OPTIMAL → back). **AK result**: `non_zero=1024/4096 first_16=000000ff…` for BOTH views, all 30 frames sampled — that's exactly the render pass's `background_color_active = (0.0, 0.0, 0.0, 1.0)` clear color, period. **hello_xr (same monado-service, same scratch ring): `non_zero=4096/4096 first_16=779797ff…`** — real grayish-blue cube content. So `compose_via_squasher` deposits real content for per-eye swapchains and only the clear-color for atlas swapchains. Trace: `comp_render_gfx_dispatch` → `comp_render_gfx_layers` runs `do_projection_layer` per view, which calls `get_layer_image(layer, sc_array_index, vd->sub.image_index)`. For atlas chains, both views' `sc_array[]` slots point at the same swapchain so the image resolves correctly; `set_post_transform_rect(layer_data, &vd->sub.norm_rect, false, &data.post_transform)` reads the correct per-view norm_rect (verified again by `pack_projection_layer` diag: view 0 norm_rect=`{0,0,0.5,1}`, view 1 norm_rect=`{0.5,0,0.5,1}`, atlas=1, sc[0]==sc[1]==`000002A01F75E9F0`); `do_projection_layer` returns success (no "Layer processing failed" in monado.log). So the bug is **between `do_projection_layer`'s descriptor-set allocation and the fragment shader's pixel write**. Two candidates: (1) `get_image_view(image, layer_data->flags, array_index=0)` returns `image->views.no_alpha[0]` — if Monado's `comp_swapchain` creates per-array-layer views sized only for per-eye dims (2144x2240 instead of full atlas 4288x2240), the right view's `uv = in_uv * 0.5 + 0.5` lands out-of-bounds → `clamp_to_border_black` → `(0,0,0,0)` → blend with cleared `(0,0,0,1)` → still `(0,0,0,1)` (alpha-only scratch we observe); (2) the draw call IS issued but the fragment shader's sample of the imported D3D11/Vulkan atlas image returns black due to a layout/access mismatch on the IPC-imported memory. **The `pack_projection_layer` diag also confirms the `handle=0x0 size=0` IPC pattern** (same as hello_xr per 2026-05-28 memory) — the IPC plumbing zeroes the native handle, but the squasher's overwrite + scratch handle is what's supposed to make it work; for AK that overwrite happens but the scratch is empty. **Bonus disproven hypothesis from same session**: the encoder's `avg_bytes_per_packet=78-1112` trend, which earlier looked like real content drifting toward black, is **EXACTLY consistent with the encoder pure-black-encoding with IDR overhead averaging down** — an H.264 IDR of pure-black at 2560×1184 is ~15-20 KB; P-frames of pure-black are ~25 bytes; the average drops as IDRs space out. The Cordon Rule: bytes/packet alone can't distinguish real-but-low-motion content from pure-black-with-IDRs. Squasher-output dump is the load-bearing diag. **Snapshots on .101**: `monado.20260530_122355.squash_ak.log` (AK clear-color-only), `monado.20260530_123759.squash_helloxr.log` (hello_xr real content). **Next experiments** (recorded under remaining `⏸` lines): (a) add a per-frame draw-call counter to the layer loop in `comp_render_gfx_layers` to distinguish "no draws issued" from "draws issued but produced black"; (b) instrument `get_image_view` to log the actual `VkImageView` dimensions for AK's atlas — if it's 2144×2240 not 4288×2240, the right view definitely samples out-of-bounds; (c) hack-test by replacing `comp_render_gfx_dispatch` with explicit per-view-region `vkCmdBlitImage` from AK's atlas swapchain to scratch (definitive: if scratch then shows AK's content, the squasher path is the bug; if still empty, the IPC-imported image itself is the issue).

  * ✅ **2×2 truth table COMPLETE 2026-05-30 ~12:10: bug is conclusively OpenXR-mode + AK combination.** Rebuilt SteamVR-mode streamer from current `master` (`cargo xtask build-streamer`, 2m37s), backed up `.101:C:\alvr\test\` binaries with `.pre-may30` suffix, deployed `ALVR Dashboard.exe`+`driver_alvr_server.dll`+`libvpl.dll`+`openvr_api.dll`+`driver.vrdrivermanifest` from `build/alvr_streamer_windows/`, kept session.json. SteamVR autostarted when Quest connected; user wore headset and reported **AK is visible** via SteamVR. So: (1) AK is not inherently broken — UE5 renders visibly; (2) the Quest's decoder + de-foveation + presentation handle AK's encoded content correctly; (3) the bug specifically lives in **OpenXR-mode + AK combination**, not in either component alone. Combined with today's earlier `hello_xr-visible-in-OpenXR-mode` finding, the 2×2 truth table fully isolates the bug: SteamVR+AK ✅, SteamVR+hello_xr (not tested today, but worked 2026-05-27), OpenXR+hello_xr ✅, OpenXR+AK ❌. **Most distinctive structural difference between AK and hello_xr in OpenXR mode: AK submits a SHARED STEREO ATLAS swapchain** (one image with `norm_rect={0,0,0.5,1}` for view 0 and `{0.5,0,0.5,1}` for view 1, both pointing at the same `xrt_swapchain`); hello_xr submits PER-EYE swapchains (separate image per view). Per earlier inspection (`d26ce8b87` extended `pack_projection_layer` diag, 2026-05-28), Monado's `do_projection_layer` DOES read `vd->sub.norm_rect` and feeds it through `set_post_transform_rect` into the descriptor set, and `layer_projection.vert:48` applies it as `uv = fma(in_uv, post_transform.zw, post_transform.xy)` — so the atlas math is correct on paper. But the path from comp_alvr → squasher (with dummy distortion target) → FFR compute pass → CUDA-interop encoder may apply per-view sampling that misses the atlas norm_rect at some stage. Next experiments (now well-scoped): (a) ffmpeg-validate the actual AK encoded bitstream — if frames look like content for the LEFT half of an atlas (clipped/squashed/wrong-half), bug is in the encoder's per-view input pickup; if frames look genuinely scene-correct, bug is in client-side presentation specific to OpenXR-mode wire format; (b) instrument `compose_via_squasher` to dump scratch image bytes BEFORE the encoder reads them (we now know `src_image_dump` was reading wrong image, but the squasher's OUTPUT scratch is what the encoder samples and the encoder side has reachable VkImage handles); (c) compare projection-layer view poses + FOVs + image_index/array_index AK-vs-hello_xr to see if AK reports something atlas-specific the encoder mishandles. Test snapshot saved on .101: `session_log.20260530_120952.steamvr_ak_works.txt`. **Durable note**: `cargo xtask build-streamer` (no `--gpl`, no `--release`) is sufficient for the SteamVR baseline test; build output at `build/alvr_streamer_windows/`. Backup of old SteamVR binaries on .101 at `.pre-may30` suffix.

  * ✅ **AK black isolated to AK-specific: hello_xr visually confirmed working under identical pipeline 2026-05-30 ~11:30.** With all today's diag patches in place, monado-service running, AK swapped out → hello_xr launched: user reported colored cubes visible in headset. So the post-encoder pipeline (encoder output → wifi → Quest decoder → de-foveation → display) is end-to-end healthy today; AK is uniquely broken. The 2026-05-29 "source swapchain empty at sample time" finding (`bdf9da388`'s `src_image_dump non_zero=0/4096`) is a **measurement artefact**, not a real bug: the dump used `oldLayout = VK_IMAGE_LAYOUT_UNDEFINED` for its TRANSFER_SRC_OPTIMAL barrier, and per the Vulkan spec UNDEFINED is explicitly allowed to discard contents — NVIDIA RTX 3090 does zero them. Tried fixing the old layout to SHADER_READ_ONLY_OPTIMAL (fork `4afb079f4`); dump still showed all-zero, suggesting the dump is reading from an image *different* from what the squasher actually samples (IPC-imported swapchain image objects are not surfaced via the path `comp_swapchain.vkic.images[img_idx]` reads from — confirmed later by hello_xr ALSO showing all-zero in the dump while being visually correct). Treat the entire `src_image_dump`-based investigation chain as **invalidated**. Real evidence: the encoder bitstream diag (`avg_bytes_per_packet`) showed `990 → 80` over a run for AK and `135 → 24` in earlier runs — these are real encoded video with declining motion content over time (UE5 stationary scene → encoder's motion estimation finds nothing → tiny P-frames; that's NORMAL H.264, not an empty-input symptom). All commits + observability stay landed but the original "empty source / multi_compositor sync race" hypothesis chain is abandoned. **Next AK-specific investigation directions** (none of which involve the wait/sync/source path now ruled out): (1) dump AK's encoded H.264 bitstream + ffmpeg-validate on the dev host — answers whether AK's encoder output is visually meaningful or whether AK is genuinely rendering near-black content; (2) check AK's PC-side mirror window — if AK isn't rendering visible content on its own desktop window either, the bug is in AK itself (loading-screen / UE5 init / OpenXR view config) not in our pipeline; (3) compare projection-layer view poses + FOVs between AK and hello_xr — frames decode but display in a position the user can't see if poses are wrong; (4) Quest STOPPING disconnect still hits AK at ~60-90s into every run (3-for-3 today, `os error 10053`/`10054`) and isn't mitigated by the v8 ConnectionLost debounce nor the client-side STOPPING debounce — orthogonal but worth instrumenting separately.

  * ✅ **multi_compositor mystery RESOLVED 2026-05-30** (fork `cc458a381` + `7142db834`, alvr `bfde7186`). Step (a) + (c) from above landed in one round: `alvr_mc_reach` at the very top of `multi_compositor_layer_commit{,_with_semaphore}` and `alvr_ipc_sync_reach` / `alvr_ipc_sem_reach` at the top of `ipc_handle_compositor_layer_sync{,_with_semaphore}` (before the `ics->xc == NULL` early-return). Re-deployed the 3-binary set to `.101:C:\alvr\test-openxr\`, re-ran AK. **In a single ~2-minute run: 72 print events each for `alvr_mc_reach path=semaphore` and `alvr_ipc_sem_reach`; zero for the fence variants. Both report `xc_nonnull=1` / `xcsem_nonnull=1` throughout.** So AK *does* reach `multi_compositor_layer_commit_with_semaphore` — it just routes through the **timeline-semaphore IPC handler**, not the import-fence one. (UE5 D3D11 → `ID3D11Fence` → OpenXR timeline semaphore → `xcsem` on the IPC side.) The 2026-05-29 "both fire zero times" finding looks attributable to a stale deploy — this run's `alvr_sem_diag` (which has been in the binary since `c923f9ee6` and wasn't touched here) fires consistently alongside `alvr_mc_reach` in the same call. **Bonus encoder-side finding from the same run**: `OpenXR encoder bitstream diag` shows `avg_bytes_per_packet=90.4 → 135.3 → 124.2` in the first stream window before the Quest disconnect at 09:52:15 (`os error 10053` again, same TCP-FIN signature from memory) — that is **real content**, not the 38-byte black floor we'd been chasing for AK. After the auto-reconnect at 09:52:36, steady-state bytes/packet drifts down to ~24 (black floor) over the next minute. So the encoder pipeline *does* deliver real foveated content when the source swapchain is non-empty; the bug is intermittent, not constant. Next experiments: (a) instrument `multi_compositor_layer_commit_with_semaphore` further to log the actual `xrt_compositor_semaphore::wait` result (does the wait_thread really block on AK's `ID3D11Fence` value?); (b) re-take the `src_image_dump` capture (`bdf9da388`) in the **early** window when bytes/packet is still high vs the **late** window when it's at the black floor — confirms whether the source-image-empty pattern correlates with the bytes/packet drop or is a separate phenomenon; (c) look at why a Quest STOPPING blip is still bringing the stream down for ~20 s despite the v8 ConnectionLost debounce + client-side STOPPING debounce. **Durable infra fix**: AK's bundled OpenXR loader honours HKLM `ActiveRuntime` (SteamVR) over HKCU on this host; explicit `XR_RUNTIME_JSON` is required. Persisted in `.101:C:\alvr\test-openxr\run_ak.cmd` (pre-fix saved as `run_ak.cmd.pre-runtime-fix`).

  * ⏸ **Original real-game investigation** — surfaced 2026-05-28 trying real-game tests (AK.exe UE5 + hello_xr from Khronos OpenXR-SDK). Both apps successfully reach `xrCreateInstance` + native session creation through Monado (`application_name: 'AK'` / `'HelloXR'` in `update_session_state_locked: Started native session, N active app session(s)`), AK at view-config = 4288×2240, hello_xr through the D3D11 timeline-semaphore handshake. **Neither reaches `xrEndFrame`** — both sit silent, no `layer_commit` / `compose_via_squasher` / `[OXR STATS]` traffic, no client decode, headset stays black. Pattern matches the documented Slice 2d trap: `alvr_hub.c::broadcast_state_change` pushes events via `u_system_broadcast_event` which iterates *currently-registered* sessions and pushes; **events are NOT replayed for sessions joining later**. So if the Quest connects → `StateChange{visible=focused=true}` broadcasts to nobody → Quest disconnects on idle → then the OpenXR app starts its session → that app sits at READY waiting for a SYNCHRONIZED kick that won't come (no new client connect event). Today's `oxr_overlay_smoke` works around this by submitting unconditionally after `xrBeginSession` (per Slice 2d trap #1 — don't gate frame submission on session state). Real OpenXR apps (UE5, hello_xr, presumably any production game) honour the state machine and stall. **Fix is in the fork**: `alvr_hub.c::alvr_create_devices` (or a Monado-side hook that fires on `xrt_session_register`) needs to push the *current* bridge state to the broadcast sink so late-joining sessions get woken. ~30-60 min: fork edit + submodule bump + rebuild + redeploy + re-test with hello_xr -g D3D11. **Until this lands, OpenXR mode only works with apps that submit unconditionally — the smoke clients we own, and presumably anything that uses Monado's headless mode.** Build artifacts available: D:\projects\OpenXR-SDK-Source@1.1.60 cloned, hello_xr.exe + openxr_loader.dll built Release at `build-win/src/tests/hello_xr/Release/` and `build-win/src/loader/Release/`, deployed to `.101:C:\alvr\test-openxr\hello_xr\`. Use `-g D3D11` (Vulkan2 binding has its own retry-loop issue with this Monado; D3D11 gets through binding setup but blocks on state). `-g D3D12` and `-g Vulkan` not tried.
* Hand-tracking input passthrough (`XR_EXT_hand_tracking`) — **scope nailed down 2026-05-22 in [`HAND_TRACKING_PASSTHROUGH.md`](HAND_TRACKING_PASSTHROUGH.md)**. Headline: the wire and `alvr_server_core` API already carry everything; the gap is on the OpenXR-mode side only (bridge ABI v4 entry + Monado-side `xrt_device` with `get_hand_tracking`). Zero `alvr_packets` change needed for the basic case (client does not rebuild).
  * ✅ **Slices 1 + 3 LANDED 2026-05-22** — alvr-side end-to-end: bridge ABI bumped 3 → v4 with `alvr_oxr_get_hand_skeleton(side, at_timestamp_ns, *out_joints, *out_is_tracked)`, the `AlvrOxrHandJoint` struct, the `ALVR_OXR_HAND_JOINT_COUNT = 26` constant, and the body wired straight to `ServerCoreContext::get_hand_skeleton` (HandType is already re-exported at the `alvr_server_core` crate root). 26 joints get written when the client has a tracked hand on this side; `out_is_tracked = false` (and out_joints left untouched) otherwise. **Hosting blocker does not apply**: `openxr/src/xrt/drivers/alvr/CMakeLists.txt:11` `target_include_directories(... PRIVATE ${ALVR_BRIDGE_HEADER_DIR})` PRIVATE-includes the alvr-side `alvr_runtime_bridge.h` directly, so both halves of the ABI check share the same cbindgen-emitted `ALVR_OXR_BRIDGE_ABI_VERSION` macro at compile time — no separate `_EXPECTED` constant to bump on the Monado side. (Both the hand-tracking and per-view-foveation scoping docs originally said there was one; corrected.) Verification: `cargo check -p alvr_server_openxr -p alvr_server_core` clean, `cargo xtask clippy --ci` clean, header regenerated via `ALVR_REGENERATE_BRIDGE_HEADER=1 cargo build -p alvr_server_openxr`.
  * ✅ **Slice 2 LANDED 2026-05-22** — Monado-side `xrt_device::get_hand_tracking`. New per-side `alvr_hand` xrt_device (`openxr/src/xrt/drivers/alvr/alvr_hand.c`) flagged `XRT_DEVICE_HAND_TRACKER` / `XRT_DEVICE_TYPE_HAND_TRACKER`, advertises one `XRT_INPUT_HT_UNOBSTRUCTED_{LEFT,RIGHT}` input, calls `alvr_oxr_get_hand_skeleton`, fills `xrt_hand_joint_set` poses 1:1 (joint enum matches `XR_HAND_JOINT_*_EXT` order), applies the static per-joint radius table via `u_hand_joints_apply_joint_width`, sets `hand_pose` to the wrist relation, leaves `is_active=false` (zeroed set) when the bridge reports untracked. `alvr_hub.c::alvr_create_devices` wires the two new devices into `xsysd->xdevs[]` and points `xsysd->static_roles.hand_tracking.unobstructed.{left,right}` at them so the OpenXR state tracker resolves them via `GET_XDEV_BY_ROLE(sess->sys, hand_tracking_unobstructed_left/right)` (see `oxr_api_session.c::OXR_SET_HT_DATA_SOURCE`). Verification: Gate A (`cargo xtask build-openxr-runtime --enable-alvr-driver`) + Gate B (`monado-service.exe` boots, reports 5 devices including `ALVR Streamed Hand Tracker (L/R)`, `hand_tracking.unobstructed.left/right` resolved, `INFO [comp_alvr_create_system_compositor] ALVR compositor ready` still present, no ABI mismatch) both green.
  * ✅ **Gate C CLEARED 2026-05-22** — end-to-end XR_EXT_hand_tracking verified Quest 3 → ALVR-Monado → PC OpenXR client at 100% joint validity. Bring-up surfaced two latent bugs that don't fire at Gates A/B; both fixed alongside the verification. (1) **alvr-side**: `ServerCoreContext::get_hand_skeleton` did exact-timestamp lookup, but Quest hand samples carry the client's predicted-display timestamp (Quest XrTime domain) while the PC caller queries with Monado-monotonic time — and there is no client/server clock sync (`client_openxr/src/stream.rs:645` "no time sync step is performed"). Now returns the most-recent sample regardless of `_sample_timestamp`; staleness bounded by Quest's ~10–16 ms send cadence. Throttled `info!` in `report_hand_skeleton` so future investigations can grep `report_hand_skeleton` to disambiguate Quest→streamer vs streamer→bridge. (2) **Monado-fork-side**: `alvr_create_devices` called `u_space_overseer_create(broadcast)` without the standard follow-up `u_space_overseer_legacy_setup(uso, xdevs, xdev_count, head, &T_stage_local, ...)`. Empty overseer → every `xrt_space_overseer_locate_device` IPC returned an error pipe with `ReadFile: 109 ERROR_BROKEN_PIPE` → every `xrLocateSpace` / `xrLocateHandJointsEXT` failed with `XRT_ERROR_IPC_FAILURE`. Client-visible symptom: `Supported reference spaces: [LOCAL]` instead of `[VIEW, LOCAL, STAGE]`. Mirrored `u_builders.c`'s standard setup (1.6 m local-floor offset) — every locate call now resolves cleanly. **Verification tool**: new `alvr/oxr_hand_smoke` crate — headless OpenXR smoke test that drives the full path; on green run reports `Summary: frames=N L valid=N (100%) R valid=N (100%)`, IPC failure count 0. See [`HAND_TRACKING_PASSTHROUGH.md`](HAND_TRACKING_PASSTHROUGH.md) and [[openxr-mode-quirks-windows]] for launch quirks (must run non-elevated on Windows, per-user `ALVR_ROOT`).
* ✅ **`XrSessionStateChanged` events plumbed end-to-end LANDED 2026-05-21.** alvr_hub now stashes the `xrt_session_event_sink *broadcast` it already received in `alvr_create_devices` and pushes `xrt_session_event_state_change` events on bridge StateChange (visible=focused=true) and ConnectionLost (visible=focused=false). The OpenXR state tracker walks READY → SYNCHRONIZED → VISIBLE → FOCUSED on its own once both flags are true (and the inverse on disconnect). ALVR's `server_core` doesn't distinguish the intermediate states, so the driver only emits the boundary transitions. Fork commit `e4d281eb2`; alvr `f84d3d04` submodule bump. Verification ceiling: behavioural verification (an OpenXR app actually receiving the events via `xrPollEvent`) is hardware-gated alongside Gate C.
* Per-view foveation hint to the OpenXR-mode encoder — **scope nailed down 2026-05-22 in [`PER_VIEW_FOVEATION.md`](PER_VIEW_FOVEATION.md)**. Headline: eye-tracking data (`FaceData.eyes_*`) already arrives in `TrackingData` but isn't piped to the encoder. Per-view variation needs the params to travel per-frame (or low-rate dynamic). Wire-compat win: zero `alvr_packets` break for the v0 case if we route via additive `RealTimeConfig.per_view_foveation: Option<[FoveationView; 2]>`.
  * ✅ **Slices 1 + 2 + 3 + 5 LANDED 2026-05-22** — alvr-side end-to-end except the producer wiring and the encoder body:
    * **Slice 1** (alvr `04e88592`) — bridge ABI bumped 4 → 5 with `alvr_oxr_get_foveation(*out_views)` (encoder hot-path read) and `alvr_oxr_set_foveation(*views)` (drain-thread write). `AlvrOxrFoveationView { is_present, center_size[2], center_shift[2], edge_ratio[2] }` mirrors `alvr_session::FoveatedEncodingConfig`. `static FOVEATION: RwLock<[AlvrOxrFoveationView; 2]>` cache initialises to `{ is_present: false, center_size: 0, center_shift: 0, edge_ratio: 1.0 }` so the encoder defaults to full-resolution. Cache lives outside `SERVER_CORE_CONTEXT` on purpose.
    * **Slice 2** (alvr `a2ecde3c`) — `FoveatedEncodingConfig` gains additive `per_view_eye_tracked: Switch<PerViewFoveationConfig>` (defaults Disabled). `PerViewFoveationConfig { update_rate_hz=10.0, max_offset_normalized=0.25, confidence_floor=0.5 }`. Embedded-engine C ABI (`alvr_start_stream_opengl`) updated to pass `Switch::Disabled` for the new field. Session test `test_per_view_eye_tracked_defaults_disabled_and_survives_old_session` pins both halves of the additive-by-default contract.
    * **Slice 3** (alvr `1d693f46`) — `ServerCoreEvent::PerViewFoveation([PerViewFoveationView; 2])` variant. OpenXR-mode drain thread in `alvr_server_openxr` translates it into the bridge cache (`*FOVEATION.write() = ...` via a `per_view_to_bridge` helper that adds `is_present=true`). SteamVR mode drops the variant silently (no per-view encoder support yet). The embedded-engine C ABI extends its existing "implementation not needed" arm.
    * **Slice 5** (alvr `b2d3bde2`) — `pub fn gaze_to_center_shift(gaze: Quat, half_fov_x_rad, half_fov_y_rad, dead_band_rad, max_offset_normalized) -> [f32; 2]`. Pure-Rust stateless math: `gaze * NEG_Z` → yaw/pitch → dead band → normalise → clamp. Sign convention matches `FoveatedEncodingConfig::center_shift_{x,y}`. No time-series smoothing — the one-Euro / EMA upstream of this call is the caller's responsibility. 8 unit tests covering identity, microjitter dead band, max clamp on both axes/signs, half-FOV → ±1 pre-clamp, zero half-FOV (no divide-by-zero), axis independence, sign cement.
  * ✅ **Producer wiring LANDED 2026-05-22** (alvr `32c2be0f`) — `PerViewFoveationEmitter` in `alvr_server_core::foveation`, instantiated once per `tracking_loop` run and called per incoming `TrackingData`. Rate-limited to `update_rate_hz` (10 Hz default, 0.1 Hz floor). Picks `eyes_social` per-eye when both sides are present, else applies `eyes_combined` to both views, else no emit. Composes `PerViewFoveationView` with `center_size`/`edge_ratio` from the static `FoveatedEncodingConfig` and `center_shift` from `gaze_to_center_shift`. Sends `ServerCoreEvent::PerViewFoveation` so the OpenXR drain thread picks it up the same way as `LocalViewParams`. Gated on `per_view_eye_tracked.enabled` — byte-stable when off. Known limitation in-code: half-FOV is a placeholder 1.0 rad until the negotiated per-view FOV from `LocalViewParams` is plumbed in (TODO marked in `foveation.rs:PLACEHOLDER_HALF_FOV_RAD`).
  * ✅ **Slice 4 LANDED 2026-05-27** (the cross-version wire bump) — `alvr_packets::RealTimeConfig` gains a trailing additive `per_view_foveation: Option<[FoveationView; 2]>`, with a new wire-side `FoveationView { center_size[2], center_shift[2], edge_ratio[2] }` (mirrors `alvr_session::FoveatedEncodingConfig` / `alvr_server_core::PerViewFoveationView`; lives in `alvr_packets` because `server_core` depends on `packets`, not the reverse). `RealTimeConfig::from_settings` populates it as `Some([view, view])` only when `foveated_encoding` is on AND its `per_view_eye_tracked` switch is enabled, else `None` (legacy single-source-of-truth path untouched, byte-stable when the switch is off). Two `alvr_packets` tests: bincode round-trip (None + Some) and "defaults omit the field". `bincode = "2"` added as a `[dev-dependencies]`. **Design note that shaped the slice**: `RealTimeConfig` is built from settings and re-sent only on change at 1 Hz (`REAL_TIME_UPDATE_INTERVAL`, diff-on-change) — it is NOT a per-frame channel, so this carries the **static baseline** the client should reproject against, not the dynamic eye-tracked centre. The dynamic per-frame centre stays server-internal (tracking_loop → `ServerCoreEvent::PerViewFoveation` → encoder bridge) exactly as before; routing dynamic values over the wire would defeat the diff and belongs to a different vehicle (see option 2/3 in `PER_VIEW_FOVEATION.md`). **Verification ceiling**: the field is *produced* by the server and *deserialized* by the client (via derive), but client-side per-view reprojection consumption is NOT wired yet — `client_openxr/src/stream.rs::update_real_time_config` still only reads `passthrough`/`clientside_post_processing`. The client reprojection is symmetric ("Symmetric undo" in PER_VIEW_FOVEATION's state table) and making it per-view is graphics work tracked separately. `cargo test -p alvr_packets` green; `cargo check -p alvr_server_core -p alvr_client_core`, `cargo clippy -p alvr_packets --all-targets`, `cargo fmt --check` all clean.
  * ✅ **Real per-view FOV plumb LANDED 2026-05-22** (alvr `439d7847`) — `ConnectionContext` gained `local_view_params: RwLock<[ViewParams; 2]>` mirroring what `server_openxr` already caches. `connection.rs`'s `ClientControlPacket::LocalViewParams` handler updates the cache before forwarding the event. `PerViewFoveationEmitter::maybe_compute` now takes `&[ViewParams; 2]` and extracts a symmetric half-FOV per axis (`max(left.abs(), right.abs())`, etc). DUMMY view params before the client reports its config yield half_fov=1.0 — same numerical behaviour as the prior placeholder, so warm-up frames don't regress.
  * ✅ **Per-frame wire (PER_VIEW_FOVEATION option 2) LANDED 2026-05-27** — `alvr_packets::VideoPacketHeader` gains `per_view_foveation: Option<[FoveationView; 2]>`, the per-frame transport (sibling to the 1 Hz `RealTimeConfig` baseline from Slice 4). Server: `ConnectionContext.latest_per_view_foveation` cache (mirrors `local_view_params`), written by `tracking_loop` alongside the `ServerCoreEvent::PerViewFoveation` emit, read by `send_video_nal` to stamp each header (`From<PerViewFoveationView> for FoveationView`). `Some` only when `per_view_eye_tracked` enabled, else `None`. Deliberate lockstep wire break (`protocol_id` gates major version only). bincode round-trip test in `alvr_packets`. **Ceiling**: client deserializes but reprojects symmetrically — per-view reprojection is a separate `alvr_graphics::stream` shader refactor (spec-constants → runtime push-constants). `cargo test -p alvr_packets` (3) + `-p alvr_server_core` (28) green; clippy + fmt clean.
  * ✅ **Client de-foveation consumer LANDED 2026-05-27** (dormant) — `alvr_graphics::StreamRenderer` gained a second `FFE_RUNTIME` pipeline that derives the de-foveation constants from a per-view `center_shift` uniform (binding 2; the push-constant block was already at the 128-byte limit), mutually exclusive with the static `ENABLE_FFE` path via override constants so the static path stays byte-identical. `render` takes `Option<[Vec2; 2]>` per-view shifts; the wire value is plumbed `VideoPacketHeader.per_view_foveation → client_core::per_view_foveation_queue → report_compositor_start → per_view_center_shift() → render`. Built only when foveated encoding is on; used only when a frame carries per-view foveation (never today). **Ceiling**: no GPU/producer here — guardrail is a `naga` parse+validate test on `stream.wgsl`; the static pipeline is untouched. This completes the client consumer side; only Slice 6 (producer) remains.
  * ✅ **Slice 6 CODE LANDED 2026-05-28 (behaviour hardware-unverified)** — the encoder now applies **per-eye** foveation. `comp_alvr`'s FFR compute pass (`ffr.comp` + `comp_alvr.c` `init_ffr`/`run_ffr`) moved `centerShift` from a baked specialization constant to a per-eye **push constant** (the push-constant range added to the FFR pipeline layout), fed each frame from `alvr_oxr_get_foveation` — exactly mirroring the client's `FFE_RUNTIME` change. The other six FFR constants (`eyeSizeRatio`/`centerSize`/`edgeRatio`) + the foveated output resolution stay session-static. When a view has no per-view update (`is_present=false`, today's default) the host pushes the static config shift, so the pass is functionally identical to the pre-Slice-6 uniform FFR. **No bridge ABI bump** — `alvr_oxr_get_foveation` already existed (v5); Slice 6 just adds a consumer. Plus the Gate H synthetic-injection hook landed: `ALVR_TEST_PER_VIEW_FOVEATION=lx,ly,rx,ry` makes `PerViewFoveationEmitter` emit a constant per-eye shift (bypassing the `eyes_*` requirement), feeding both the encoder cache and the client wire. **Verification ceiling**: alvr-side `cargo test`/`clippy`/`fmt` green; Monado-side compile-checked via `build-openxr-runtime`; the *behaviour* (per-eye warp ⇄ client inverse, artifact-free both eyes) is unverified and gated on [`SMOKE_TESTS.md`](SMOKE_TESTS.md) **Gate H** on the RTX 3090 (no eye-tracked headset / GPU on the dev host).

## Open decisions still on the table

| | Decision | Currently | Revisit when |
| --- | --- | --- | --- |
| F1 | Driver shape — in-tree (`openxr/src/xrt/drivers/alvr/`) vs out-of-tree overlay | in-tree | submodule conversion |
| F2 | Frame ingress path — fake compositor vs custom `comp_target` | fake compositor (`comp_alvr.c`) | Phase 3.2 |
| F3 | Build wiring — standalone CMake vs subsumed into xtask | standalone CMake invoked by xtask | never (locked) |
| F4 | Bridge header — cbindgen vs hand-maintained | cbindgen 0.29 + `Config` in `build.rs`, opt-in regen via `ALVR_REGENERATE_BRIDGE_HEADER=1` (resolved 2026-05-20) | only revisit if migrating to a `cbindgen.toml` file to match other crates |
| F5 | Submodule architecture — fork branch / patch overlay / upstream PR | Option A (fork branch). Local prep done 2026-05-20 (PHASE2_MANIFEST.md / phase2_alvr.patch / convert_to_submodule.sh). Awaits fork creation. | when maintainer has pushed the fork's alvr branch |

## Files a future session should read first

1. `/openxr-migration.md` — master plan with phase breakdown and risk list
2. `/docs/monado-notes/NEXT_STEPS.md` — **this file**
3. `/docs/monado-notes/SUBMODULE_PIN.md` — exact `git submodule` migration commands
4. `/docs/monado-notes/XRT_INTERFACES.md` — the Monado contracts the new code implements
5. `/docs/monado-notes/INTEGRATION_NOTES.md` — the ALVR ↔ Monado mapping table
6. `alvr/server_openxr/src/lib.rs` — the 10 stubs to fill in
7. `openxr/src/xrt/drivers/alvr/alvr_hub.c` — the driver entry, currently returns identity poses
8. `openxr/src/xrt/compositor/alvr/comp_alvr.c` — the fake compositor stub
9. `alvr/server_openvr/src/lib.rs` — the reference impl to mirror in `server_openxr`

## How to not break the existing OpenVR mode

Three rules that keep `master` shippable while Phase 3 work is in flight:

1. **Default `RuntimeMode` stays `Steamvr`.** Never change `RuntimeModeDefault::variant` in `settings.rs`.
2. **`XRT_BUILD_DRIVER_ALVR` and `XRT_FEATURE_COMP_ALVR` stay OFF by default in `openxr/`'s CMake config.** Both gates must be flipped explicitly via `cargo xtask build-openxr-runtime --enable-alvr-driver`.
3. **Touch `alvr/server_openvr/cpp/` only for the encoder refactor (3.0), and only to extract a runtime-agnostic interface. Do not change the OpenVR-facing wrapper.**

If those three hold, `cargo xtask build-streamer` keeps producing a working SteamVR driver no matter what state Phase 3 is in.
